# Dungeon Hunter 2 (Gameloft) — PS Vita Port Plan

> Written in English per project convention for shareable documentation. Prior-art ports referenced
> below (`Zenonia2-vita`, `Zenonia3-vita`) keep their working notes in Spanish; this document does not
> duplicate their content, it distills what's reusable from them.

## 0. Prior art in this workspace

Three sibling ports live next to this one and are the reason this project doesn't start from zero:

- **`../Prince of Persia `** (note the trailing space in the folder name) — the **origin of the
  SoLoader + FalsoJNI boilerplate** used by every port in this workspace, targeting a cocos2d-x game.
  `psvita-port-toolkit/` in this repo is the genericized distillation of that port (toolchain setup,
  `so_util` dynamic loader, FalsoJNI, hardware debugging methodology, LiveArea/VPK packaging rules).
  Its `porting_tools/` directory is also the direct ancestor of this repo's `porting_tools/`.
- **`../Zenonia2-vita`** — first port in the family to reach a **playable state on real hardware**
  (menus navigable, game start-able) using the Gamevil "Nexus2/Clet" engine. `port_progress.md` there
  documents ~13 real hardware bugs and fixes — the JNI-stub, pthread, and file-hook lessons apply to
  *any* SoLoader port regardless of engine, not just Gamevil titles.
- **`../Zenonia3-vita`** — forked directly from Zenonia2-vita's proven codebase because it uses the
  **same engine**. Its `plan_zenonia3_port.md` is a good model for *how to structure this kind of
  document* (a "what's identical / what changed" comparison table before writing any loader code), but
  its actual ABI conclusions don't transfer here — Dungeon Hunter 2 is a **different, proprietary
  Gameloft engine**, not Gamevil Nexus2 and not cocos2d-x.

**What transfers directly from all three:** the generic SoLoader architecture (`so_util.c/h`),
FalsoJNI (vendored from Prince of Persia, MIT-licensed), the hardware-debugging methodology (one log,
one bug at a time), the input-handling lessons (touch sampling off by default, never pass raw Vita
touch IDs to the engine), and the LiveArea/VPK packaging rules. **What does not transfer:** any
specific JNI method table, ABI quirk, or binary patch offset from Zenonia — those are specific to
Gamevil's engine and must be independently re-derived for Gameloft's engine (see Phase 1 below, already
done).

## 1. Target — static analysis (Phase 1, completed)

Package: `com.gameloft.android.GAND.GloftD2SS`, main activity `DungeonHunter2`. Two libraries are
loaded via `System.loadLibrary`, in this exact order (confirmed in `DungeonHunter2.java`):

```java
System.loadLibrary("DungeonHunter2");
System.loadLibrary("StormGLOFT");
```

A third library, `libnativeinterface.so`, is loaded independently by `Zirconia_DRM.java` and is
**unrelated to game logic**.

### `libDungeonHunter2.so` (`lib/armeabi-v7a/`, ~15.9 MB, **not stripped — has symbol/debug info**)

This is the actual game engine + JNI bridge, and the fact that it isn't stripped is a major advantage
over the Zenonia ports (both stripped): real C++ class and function names are available directly from
`nm`/`objdump`, no guesswork needed. 32 JNI exports found, across 6 classes:

| Java class | Native methods |
|---|---|
| `DungeonHunter2` | `nativeInit`, `nativeAccelerometer`, `nativeCanInterrupt`, `nativeGetGameMusicVolume`, `nativeGetInfo`, `nativeKeyDown`, `nativeKeyUp`, `nativeOpenIGM`, `nativePause`, `nativeResume`, `nativeSetOrientation`, `nativeSetPhone`, `nativegetState`, `nativeonTrackballEvent` |
| `GameGLSurfaceView` | `nativeOnTouch` |
| `GameRenderer` | `nativeGameRenderer`, `nativeConfig`, `nativeDone`, `nativeGetJNIEnv`, `nativeInit`, `nativeOnDrawFrame`, `nativeOnSurfaceChanged`, `nativeRender`, `nativeResize` |
| `GLMediaPlayer` | `nativeInit`, `nativeGetTotalSounds`, `nativeGetTotalSoundsOfSameInstance`, `nativeSetStopOnMusic` |
| `GLResLoader` | `nativeInit` |
| `GLUtils_Device` | `nativeInit` |
| `Musicplayer` | `nativeInitplayer`, `nativeDisplayMusicTitle` |

This is a **standard `GLSurfaceView.Renderer` bridge**, not a cocos2d-x `nativeInit/nativeRender` pair
nor Gamevil's `handleCletEvent` single-channel input — expect a different lifecycle: `GameRenderer`
drives `onSurfaceChanged`/`onDrawFrame` like any Android `GLSurfaceView`, and `DungeonHunter2` handles
the rest (keys, touch via a *separate* `GameGLSurfaceView.nativeOnTouch`, accelerometer, phone-call
interruption via `nativeSetPhone`/`nativeCanInterrupt`, and an in-game-menu hook `nativeOpenIGM`).

**Two license-check subsystems found inside this binary — both must be bypassed, not implemented:**
- A full C++ `ALicenseCheck` class (`ALicenseCheck::ValidateServer`, `sendRequestByGet`,
  `getIMEI`, `createUniqueCode`, XOR-based response validation) — Gameloft's own server-side license
  check, keyed off device IMEI. There is no IMEI on Vita and the validation server is almost certainly
  gone; this path must never be reached (stub the JNI entry point that triggers it, or force
  `ValidateNative()`'s stored result to "valid" before it's read).
- References to Google Play LVL-style checks are present as dead code paths (not directly exported as
  JNI) — same treatment: never call, never link against a real Play Store.

### `libStormGLOFT.so` (`lib/armeabi-v7a/`, ~907 KB, stripped, **no JNI exports**)

Initial assumption (from the name, evoking Gameloft's "Storm3D" engine branding) was that this is a
rendering/engine library. **Static analysis disproves that.** Exported symbols
(`ZzBuildHook`, `ZzEnableHook`, `HookArm`, `HookThumb`, `BuildArmJumpCode`, `BuildStubThumb`,
`GetARMInsnType`, trampoline builders, etc.) identify it as an **ARM/THUMB inline-hooking framework**
("Zz" hooking, the same family as pattern seen in various Android anti-tamper libraries) — this is
Gameloft's runtime anti-piracy/anti-tamper layer, hooking libc/Dalvik entry points at runtime to detect
repackaging or emulation. It exports `JNI_OnLoad` but is loaded purely for its side effects (installing
hooks), not because the game calls exported JNI methods on it.

**Decision: do not port this library's behavior.** A soloader environment is not the Android runtime
this hooking code expects (no real Dalvik, no real GOT layout to hook safely), and there is nothing in
it the game needs functionally — it is anti-piracy instrumentation, not gameplay code. Two options,
cheapest first:
1. Don't load it at all (`System.loadLibrary("StormGLOFT")` becomes a no-op in `java.c`'s
   `loadLibrary` hook). If nothing else in `DungeonHunter2.java` calls a native method that only this
   `.so` would have resolved (none currently found — its own exports are internal, not JNI), this is
   safe.
2. If skipping it entirely turns out to break something unexpected (confirm on real hardware before
   assuming), load it but stub its `JNI_OnLoad` to a no-op that just returns the JNI version, never
   letting the hook-installation code run.

### `libnativeinterface.so` (both `lib/armeabi/` and `lib/armeabi-v7a/`, ~11 KB, **byte-identical
MD5 in both ABI folders**, stripped)

Exports `Java_com_samsung_zirconia_NativeInterface_*` (`checkLicenseFile`, `checkLicenseFile2`,
`storeLicenseKey`, `doPassphraseTest`) plus an embedded SHA1 implementation. This is **Samsung Zirconia
DRM** — a Samsung-Apps-store-specific licensing mechanism, loaded from `Zirconia_DRM.java`, completely
unrelated to Gameloft's engine or to Google Play. **Decision: never load this library; stub the Java
call site's result (or the 4 JNI methods, if `Zirconia_DRM.java`'s call path can't be short-circuited
in `java.c` before it needs a real function) to whatever value makes `Zirconia_DRM` treat the license
as already valid.** Read `decompiled/apk_jadx/sources/.../Zirconia_DRM.java` to confirm exactly what
value each stub must return before writing the stub — do not guess the semantics from the function
names alone.

## 2. Decompilation — reproducible, one artifact per folder

Per project convention (see `.gitignore` — none of this is committed, all of it is regenerable),
everything lives under `decompiled/`, **one subfolder per artifact** so JNI/pseudo-C output never
collides across ABIs:

```
decompiled/
├── apk_jadx/                              # jadx: classes.dex + resources → Java sources
├── libnativeinterface_armeabi/            # nm/objdump symbol dump + Ghidra/angr pseudo-C
├── libnativeinterface_armeabi-v7a/        # (byte-identical binary to the armeabi copy, decompiled anyway for completeness)
├── libDungeonHunter2_armeabi-v7a/
└── libStormGLOFT_armeabi-v7a/
```

Reproduce with `porting_tools/build/decompile_all.sh` (already adapted to this project's paths — see
§4). Manually, the two techniques it wraps:

```bash
# Java (fast, no Docker needed if jadx is installed locally)
jadx -d decompiled/apk_jadx "Dungeon-Hunter-2-HD-v1-0-2.apk"

# Per .so: quick symbol triage first (no ARM toolchain needed — system objdump/nm read dynsym fine)
nm -D --defined-only "<lib>.so" | c++filt
objdump -T "<lib>.so" | grep UND        # imports
objdump -T "<lib>.so" | grep "Java_"    # JNI exports

# Full pseudo-C via Ghidra headless + angr, in Docker (devrvk/so-decompiler)
docker run --rm --platform linux/amd64 \
  -v "<lib_dir>:/input" -v "decompiled/<lib>_<abi>/ghidra:/output" \
  devrvk/so-decompiler decompile /input/<lib>.so /output
```

Note: `libDungeonHunter2.so` is 15.9 MB and not stripped, so its Ghidra pass takes noticeably longer
than the Zenonia binaries (tens of minutes under `--platform linux/amd64` emulation on Apple Silicon,
vs. under a minute for the two `libnativeinterface.so` copies). Budget time accordingly and run it in
the background rather than blocking on it.

## 3. Repo hygiene — DMCA prevention

Nothing derived from Gameloft's copyrighted binaries goes to GitHub. `.gitignore` at the repo root
excludes, at minimum:
- The original `.apk`/`.zip`/cache archive and the raw extracted APK tree (`Dungeon-Hunter-2-HD-v1-0-2/`).
- The installed-app data dump (`com.gameloft.android.GAND.GloftD2SS/` — savegames, videos, proprietary
  `.bdae`/`.bar` assets).
- The entire `decompiled/` tree (jadx Java sources and Ghidra/angr pseudo-C are both derivative works
  of the original binary — regenerable any time via `porting_tools/build/decompile_all.sh`, never
  committed).
- Any staged/repackaged asset directory for the Vita build (`ux0_data/`), the vendored proprietary
  `.so` once copied next to the loader for symbol resolution during development (`lib/**/*.so` — the
  loader's *own* code such as FalsoJNI is `.c`/`.h`, not `.so`, so this rule doesn't catch it), crash
  dumps (`*.psp2dmp`), and per-run debug logs (`/logs/`, `log_*.txt`).

Only the loader's own source code, build scripts, the toolkit/skill documentation, and small factual
artifacts (symbol name lists, this plan) are meant to be committed.

## 4. Toolkit and tooling — adapted, not reused verbatim

- **`psvita-port-toolkit/skills/psvita-porting/`** is installed at `.claude/skills/psvita-porting/`
  so Claude Code activates it automatically for this project (per the toolkit's own README). Read
  `psvita-port-toolkit/PORTING_GUIDE.md` once as the general map of phases 0–9; treat its checklist as
  the definition of done for a first playable build.
- **`porting_tools/`** (forked from Prince of Persia's toolset, already reorganized into
  `automation/`, `build/`, `misc/`, `tests/`, and already parameterized for this project — see
  `manage_vita.py`'s `VITA_IP`/`VITA_DATA_DIR`/`VITA_LOGS_DIR` constants and `build/decompile_all.sh`'s
  `BASE_DIR`) is the deployment/debugging harness: build+install, FTP upload/log-pull via
  `manage_vita.py`, Vita3K fast-iteration via `build/deploy_and_launch_vita3k.sh`, crash-dump retrieval
  via `misc/get_dump.sh`, and the click-automation scripts under `automation/` for driving Vita3K's Qt
  UI in tests. None of these needed inventing from scratch — they needed their hardcoded paths/IDs
  pointed at *this* project instead of Prince of Persia's, which is now done.
- **Build-flag TUI system ported from `Zenonia4-vita`'s `porting_tools/` (2026-07-24)**, adapted for
  DH2 instead of copied verbatim (that port's own flags are specific to its cocos2d-x-style CPU
  framebuffer-blit engine — `RGB565_DIRECT_UPLOAD`, `DIRTY_RECT_UPLOAD`, etc. — none of which apply to
  DH2's real GLES2 3D pipeline). New `build.sh` at the repo root (previously the rsync+cmake+make logic
  lived inline inside `porting_tools/build/build_and_install.sh` with no way to build more than one
  named variant); the wrapper script now discovers `build.sh`'s real flags dynamically (greps its own
  `"$1" = "--xxx"` conditions) and prompts with a short description per flag, so the menu can't drift
  out of sync with the flags. Currently exposes `--no-vsync-test`/`--downsample-test` (see Phase 11) as
  the first two flags — same pattern Zenonia4 used for its own perf-investigation phase, just with
  DH2-appropriate flags. Each variant builds to its own named `.vpk`/`.elf`/`eboot_<name>.bin` under
  `build/`, never overwriting another variant. Also ported: `manage_vita.py` now runs `parse_dump.py`
  automatically right after downloading a crash dump (previously a separate manual step), and its
  "upload eboot only" shortcut lets you pick among all `eboot_<variant>.bin` files instead of assuming
  one canonical `eboot.bin` (see Phase 11's tooling-bugs note for why that assumption was unsafe).
- Before writing loader code, copy the **generic, non-game-specific** pieces from
  `Zenonia2-vita`/`Zenonia3-vita` if a from-scratch SoLoader skeleton is wanted as a starting point
  (`so_util.c/h`, vendored `FalsoJNI`, the general shape of `dynlib.c`'s hook table) — but do **not**
  copy their `java.c` method tables or `main.c` init-call sequence verbatim; this engine's JNI surface,
  init order (`GameRenderer` vs `DungeonHunter2` vs `GLResLoader` `nativeInit` calls), and license-stub
  requirements are all different and documented in §1 above.

## 5. Phased implementation plan

Adapting `psvita-port-toolkit/PORTING_GUIDE.md`'s generic phases to what's specific here:

### Phase 0 — Toolchain — ✅ confirmed on this machine (2026-07-12)
This machine already has one shared VitaSDK install used by every sibling port, so most of this phase
was verification, not setup:
- **VitaSDK**: installed at `~/vitasdk` (`arm-vita-eabi-gcc`/`g++` 10.3.0 present under
  `~/vitasdk/arm-vita-eabi/bin`). No `$VITASDK` env var is exported globally — every sibling
  `build.sh` detects it at run time (`$VITASDK` → `/usr/local/vitasdk` → `~/vitasdk` fallback) rather
  than relying on shell profile state; this project's own `build.sh` (Phase 2) must do the same.
- **`vitaGL`** and **`kubridge` (userland stub only)**: both already built and installed via `vdpm`
  (`~/vitasdk/arm-vita-eabi/lib/libvitaGL.a`, `libkubridge_stub.a`, `libkubridge_stub_weak.a`,
  headers present). No pinned commit hash could be recovered from `~/vitasdk-src/bootstrap.log` (not
  logged there) — if a vitaGL-version-specific bug shows up later, rebuild from a known commit via
  `vdpm` at that point and record it here; don't assume today's binary is any particular commit.
- **`kubridge` kernel plugin (`.skprx`)**: **not present anywhere in this workspace** — only the
  linking-time stub is installed. The actual taiHEN plugin has to be installed on the physical test
  console (`ux0:tai/config.txt` + the `.skprx` from the kubridge GitHub releases) before hardware
  bring-up (Phase 10); this is a device-side, one-time step, out of scope until then.
- **`libshacccg.suprx`**: copied into this repo's root from `Zenonia2-vita/` (Sony's shader compiler,
  not game-specific, shared across every vitaGL project here — gitignored via `*.suprx`, same as the
  sibling repos, since it's Sony's own binary, not ours to redistribute). Still needs to land in
  `ur0:data/` on the physical test console before first run; not done yet — **the test console at
  `192.168.3.15` (per `porting_tools/manage_vita.py`) did not respond to a ping on 2026-07-12**, so
  confirm it's powered on and on the network before the first deploy attempt.
- **Space-in-path workaround**: this project's path (`.../PSVITA Develop/Dungeon-Hunter-2-vita`)
  has the same space-in-path problem as every sibling port. The proven fix used by both
  `Zenonia2-vita/build.sh` and `Zenonia3-vita/build.sh` is **not a symlink** but an `rsync` of the
  whole project tree into a space-free `/tmp/<project>-src`, with `/tmp/<project>-build` as the CMake
  build directory, then copying the resulting `.vpk`/`eboot.bin` back into `<project>/build/`. Phase 2
  should copy that exact pattern into this project's own `build.sh` rather than re-deriving it.

### Phase 1 — Static analysis — ✅ done, see §1
Re-open `decompiled/libDungeonHunter2_armeabi-v7a/ghidra/out_ghidra.c` when a specific function's
internals are needed (e.g. what `GameRenderer::nativeOnDrawFrame` actually draws, or the exact
`ALicenseCheck` call site to short-circuit) — the symbol dumps in §1 tell you *what* exists, the
pseudo-C tells you *how it behaves*.

### Phase 2 — Loader bootstrap — ✅ compiles and packages (2026-07-12/13)
Superseded plan: this repo's actual loader foundation, dropped in at the repo root during this same
work session, is **not** a fork of the Zenonia/Prince-of-Persia lineage — it's Volodymyr Atamanenko's
"SoLoBoP" (So-Loader Boilerplate, MIT), a more current and considerably more complete generic base
(`source/main.c`, `source/dynlib.c`, `source/java.c`, `source/patch.c`, `source/reimpl/*` — EGL, errno,
io, log, mem, pthread, sys, time64, an `AAssetManager` reimplementation — `source/utils/*`, plus
`lib/so_util`, `lib/libc_bridge` (NID-based SceLibcBridge for stdio), `lib/fios`, `lib/sha1`,
`lib/kubridge`). `lib/falso_jni/` was vendored from `Zenonia3-vita` (same MIT FalsoJNI, this repo's copy
was still an empty placeholder). Done so far:
1. **Vendored `lib/falso_jni/*`** (FalsoJNI.c/h, ImplBridge, Logger, MIT) — the only piece of the SoLoBoP
   skeleton that was still missing/empty.
2. **`CMakeLists.txt`** adapted: `project(dungeon_hunter_2)`, `VITA_TITLEID "PSVDH0002"`,
   `DATA_PATH "ux0:data/dungeon-hunter-2/"`, `SO_PATH "${DATA_PATH}libDungeonHunter2.so"` (only this one
   library is loaded — see §1: `libStormGLOFT.so` is anti-tamper hooking code with no JNI exports,
   `libnativeinterface.so` is unrelated Samsung DRM; neither is in the `add_executable` source list or
   ever passed to `so_file_load`), `PSVITAIP` matching `porting_tools/manage_vita.py`'s `192.168.3.15`.
3. **`source/dynlib.c`**: this template's own `default_dynlib[]` table is already a large, generic,
   well-maintained import resolver (EGL, full GLES2, pthread, `AAssetManager`, etc.) — cross-checked
   1:1 against `decompiled/libDungeonHunter2_armeabi-v7a/symbols.txt`'s real import list and found only
   ~28 genuine gaps (soft-float `__aeabi_*` helpers, `_ZSt7nothrow`/`_ZnajRKSt9nothrow_t`, `__assert2`,
   `__isfinitef`, `uname`, `cosh`/`modff`/`inet_addr`/`wcscat`/`wcsncmp`, and a `JAVA_SOUNDS` **data**
   symbol of unconfirmed size — all added, see the "Dungeon Hunter 2" section near the end of the file).
   No `AAsset_*`/`AAssetManager_*` symbols are imported by this binary at all, confirming asset loading
   goes through the JNI callback path (`GLResLoader`'s static methods), not the NDK C API this template
   also supports — important for Phase 3/8.
4. **`source/main.c`**: real init/frame/input sequence, transcribed directly from
   `decompiled/apk_jadx/sources/.../DungeonHunter2.java`, `GameGLSurfaceView.java`, `GameRenderer.java`
   (not assumed from any sibling port's engine):
   `nativeSetPhone(960,544)` → `GameRenderer` ctor (`nativeGameRenderer()` then `nativeConfig()`) →
   `gl_init()` → `nativeGetJNIEnv()` → `DungeonHunter2.nativeInit(0)` (full game, not demo — see the
   function for why) → `GameRenderer.nativeInit(1)` → `nativeOnSurfaceChanged(960,544)` → per-frame loop
   (`nativeKeyDown`/`nativeKeyUp` from a single most-recent-key queue exactly like the original Java,
   `nativeOnTouch(type,x,y,pointerId,0,0)` scaled from the 1920x1088 touch panel, `nativeRender()` —
   **not** `nativeOnDrawFrame`, which the real Java never calls despite the .so exporting it) →
   `nativePause`/`nativeResume` on exit via `nativeCanInterrupt()`.
5. **Verified with a real build**, not just read: `cmake -DCMAKE_POLICY_VERSION_MINIMUM=3.5` +
   `make` from a space-free `/tmp` copy (see Phase 0) compiles, links, converts to a Sony ELF, and
   packages a `.vpk` end-to-end with no errors. Two real link-time bugs were caught and fixed this way
   (not from static reading): `__dso_handle` must be `extern`, not redefined (`crtbegin.o` already
   provides it — duplicate symbol at link time), and the vendored `FalsoJNI_Logger.c` calls a
   `game_log()` function that only existed in the Zenonia lineage's `main.c` — added a small shim in
   this project's `main.c` forwarding to `l_info()`.
6. `porting_tools/build/build_and_install.sh` was still an unadapted copy of Prince of Persia's own
   script (`popclassic.vpk`, `EMULATOR_BUILD`/`ENABLE_VERBOSE_LOG` cmake flags that don't exist in this
   project's `CMakeLists.txt`) — rewritten to match this project's actual build (rsync-to-`/tmp`,
   `dungeon_hunter_2.vpk`, `CMAKE_BUILD_TYPE=Debug/Release` instead of those Prince-of-Persia-specific
   options).

**Not done yet (do not assume it works until tested):** `source/java.c`'s FalsoJNI method table is
still the SoLoBoP template's empty default — the `GLResLoader` static-method asset bridge (Phase 3/8)
is unimplemented, so any asset read will currently log "method ID not found" and return whatever
FalsoJNI's default is for that return type. Nothing has been run on Vita3K or real hardware yet — the
init call order and touch coordinate space above are informed guesses from reading the real Java, not
confirmed by a log from an actual run.

### Phase 3 — FalsoJNI method resolution — ✅ implemented, builds clean (2026-07-13)
`GameRenderer` turned out to call back into "Java" **zero** times (`nativeGameRenderer`/`nativeConfig`/
`nativeOnDrawFrame` are empty stubs in the real binary, confirmed in `out_ghidra.c`; `nativeGetJNIEnv`
only stashes the global `mEnv` every other callback uses). The actual "engine calls into Java" surface
is the other three classes, and every single method + exact signature was cross-checked against **three
independent sources** (not guessed from jadx alone, per `AI_WORKFLOW.md`'s "verify on the actual
artifact" rule):
1. jadx-decompiled Java (`GLResLoader.java`, `GLMediaPlayer.java`, `Musicplayer.java`,
   `DungeonHunter2.java`) for candidate method names/behavior.
2. `strings libDungeonHunter2.so`, which exposes the cached-jmethodID variable names the real binary
   uses (`mMethod<Name>` for `DungeonHunter2`/`Musicplayer`, `<name>ID` for `GLResLoader`/
   `GLMediaPlayer`) — this is the definitive list of what's *actually* called, as opposed to every
   static method jadx happens to show.
3. `decompiled/libDungeonHunter2_armeabi-v7a/ghidra/out_ghidra.c`'s four `*_nativeInit` functions, which
   give the literal name + JNI signature string passed to `GetStaticMethodID`, plus the `nativeXxx`
   wrapper bodies confirming argument count/order and which `CallStatic{Void,Int,Object}Method`
   variant is used.

That cross-check caught a real mismatch against the jadx output: **`GLResLoader` only ever gets 3
callbacks from native** — `getResourceFull(String)`, `getResourceBytes(String,int,int)`, and
`getResourceLength(String)`, all keyed by asset path — not the ~10 static methods
`GLResLoader.java` declares. `getResourceOpen`/`Read`/`Close`/`Skip`, `getRawResource`,
`copyResourceFromAssets`, `copyMovieFileFromAssetsToTMP`, and `getString` are Java-internal helpers
(used by `copyMovieFileFromAssetsToTMP`'s own body) that native code never calls back — confirms the
Phase 2 finding that asset loading is a 3-method String-keyed bridge, not the NDK `AAssetManager` path.

Implemented in `source/java.c` (65 methods total, `enum`-named IDs instead of Zenonia-style bare
numbers given the larger table):
- **`GLResLoader`'s 3 asset methods**: real file I/O against `DATA_PATH"assets/<name>"` (same
  convention as `source/reimpl/asset_manager.cpp`), not stubs — correctly reports "not found" until
  Phase 8 stages real assets there, rather than returning a stale/garbage sentinel for what will become
  a load-bearing path.
- **`GLMediaPlayer`'s 28 sound/movie methods** and **`Musicplayer`'s 9 playlist methods**: safe
  no-ops/neutral values (0 playlists, "not loaded", "not playing") ahead of the real Phase 7 audio
  implementation — every numeric method has an explicit registered value so none of them fall through
  to FalsoJNI's "not found" sentinel.
- **`DungeonHunter2`'s 25 misc methods**: neutral defaults matching what the real Java does on a fresh
  install/unrecognized device (`Get_PhoneLanguage`→English, `isSupportMM`→-1 matching the real Java's
  own dead-code branch, `DisableLaunchGame`→0, etc.), no-ops for UI hooks not implemented yet (Gameloft
  Live, IGP cross-promo, trophies), and a real `sceKernelExitProcess(0)` for `Exit()`. The 9 `VZ*`
  methods are a **Verizon-carrier-specific IAP/billing path** (`DungeonHunter2.java` delegates to a
  separate `VZBilling` class) — treated as a trap the same way `ALicenseCheck` is (§1): never
  implemented for real, just reported as "never in progress / already errored" so nothing can get stuck
  waiting on or retrying it.

**Verified with a real build**, not just read: full `cmake && make` from the `/tmp` rsync copy (Phase 0)
compiles `java.c` with zero warnings and produces a `.vpk` end-to-end. Not yet run on Vita3K/hardware —
that's Phase 10; the FalsoJNI method table being complete and non-crashing at build time doesn't confirm
runtime behavior against the real engine.

### Phase 4 — License/DRM bypass — ✅ done (2026-07-13)
Before chasing rendering or input bugs, make sure nothing in `DungeonHunter2.nativeInit` blocks waiting
on `ALicenseCheck::ValidateServer` or the Samsung Zirconia stub. Confirm in the log that these paths
are either never reached or return immediately with a "licensed" result.

### Phase 5 — Graphics (`GameRenderer` GLSurfaceView bridge) — ✅ confirmed GLES2 (2026-07-13)
Confirm GLES version/profile actually used (`glShaderSource`/`glGetShaderiv` are imported by
`libStormGLOFT.so` if kept — check whether `libDungeonHunter2.so` itself imports fixed-pipeline-only
symbols like the Gamevil/cocos2d-x titles, or GLES2 shader entry points; this changes the wrapper
strategy significantly and must be confirmed from the actual import list in
`decompiled/libDungeonHunter2_armeabi-v7a/symbols.txt`, not assumed from the other ports).

**Outcome:** Confirmed via `objdump -T` that `libDungeonHunter2.so` exclusively uses GLES2 programmable pipeline functions (`glCreateShader`, `glCompileShader`, `glLinkProgram`, `glUseProgram`, `glVertexAttribPointer`). No fixed-pipeline GLES1 functions (`glMatrixMode`, `glEnableClientState`, etc.) are imported.

**⚠️ Superseded 2026-07-15/18 — the "map to `vitaGL`" conclusion above turned out to be wrong in practice, not in the GLES2-detection itself.** `vitaGL` on this machine's VitaSDK is a **Cg-only build with no working GLSL→Cg translator** (`glsl_utils.o` is effectively empty — see the `backstab-vita-reference` memory). The engine feeds it real GLSL ES 1.00 source (see the `shaders.pak` finding below), which shark rejects outright. Two translation strategies were tried and abandoned before the current approach:
1. vitaGL's own semantic-binding GLSL→Cg path — non-functional in this SDK build (no translator compiled in).
2. Manual/AI-assisted per-shader GLSL→Cg translation (`porting_tools/manage_vita.py`'s `download_glsl_shaders`/`upload_cg_shaders`/`sync_shaders`, dumping runtime `glShaderSource` calls to `glsl_dump/<sha1>.glsl` and hand-writing a matching `assets/cg/<sha1>.cg`) — labor-intensive and never finished (only 4 of 7 dumped shaders were ever translated, and those 7 are a small sample of everything the engine actually needs — see below).

**Current, correct approach (in place as of 2026-07-15): dropped `vitaGL` entirely, switched to `PVR_PSP2`** — the real PowerVR SGX543 GLES2 driver (`libIMGEGL.suprx`/`libGLESv2.suprx`/`libpvrPSP2_WSEGL.suprx`/`libGLESv1_CM.suprx`/`libgpu_es4_ext.suprx`, vendored under `modules/`), which has a **real GLSL ES compiler**. No translation is needed at all — `dynlib.c`'s `glShaderSource`/`glCompileShader` entries now resolve directly to the real driver's exported functions (confirmed: `source/patch.c` only does license-check hooking, no shader-related patching remains; `source/utils/glutil.c` has no Cg compile step, just EGL/PVR_PSP2 init). `CMakeLists.txt`'s `SHADER_FORMAT` was still stuck at `"CG"` (a stale leftover from the abandoned approach, with no code left that even reads `USE_CG_SHADERS`) — corrected to `"GLSL"` 2026-07-18 for documentation accuracy; it was already a no-op either way.

**Major finding 2026-07-18 — the engine's complete, human-readable GLSL ES source is directly available, no runtime dumping/translation required.** `com.gameloft.android.GAND.GloftD2SS/files/shaders.pak` (present in the installed-app-data dump, confirmed via a real device `fopen()` in the logs: `ux0:data/dungeon-hunter-2/shaders.pak`) is a **stored (uncompressed) ZIP archive** containing 34 cleanly-named `.glsl` files (`DepthCubeFP.glsl`, `GL_Diffuse_L1_iPhone_VS.glsl`, `UnlitTexturedFP.glsl`, etc.) plus two empty marker files `cg.config`/`glsl.config`. This is the real shader source the native engine reads and feeds to `glShaderSource` — confirmed by `strings` on the real `.so`, which references `shaders.pak` and a `PinkBadShaderFS/VS` fallback pair (Gameloft's "missing shader" placeholder, not present in the pak — must be a hardcoded fallback string in the binary, not something we need to supply). The 7 previously runtime-dumped `glsl_dump/*.glsl` (SHA1-named) are a small, incomplete subset of what the engine actually needs — **prefer extracting the full `shaders.pak` over continuing the old dump/translate loop**, which should be considered obsolete now that `SHADER_FORMAT` is GLSL end-to-end. All 34 shaders are well-formed, precision-qualified GLES2 GLSL (some behind `#define GLITCH_OPENGLES_2`) that a real PowerVR GLES2 compiler should accept unmodified.

**Bug found 2026-07-18 — `module/` vs `modules/` path mismatch (root cause confirmed on real hardware, then correctly identified and fixed).** The repo had two directories with byte-identical PVR `.suprx`/`.a` payloads, `module/` (singular) and `modules/` (plural, an untracked duplicate). Mid-session the user deleted `module/` and asked to standardize on `modules/`; all three references (`CMakeLists.txt` VPK packaging, `main.c`'s `sceKernelLoadStartModule` calls, `glutil.c`'s `PVRSRV_PSP2_APPHINT` paths) were updated to `modules/` and rebuilt. **This was then tested on real hardware (`log_054.txt`, 2026-07-18) and it was NOT sufficient**: all 5 `LoadStartModule` calls succeeded, `PVRSRVInitializeAppHint`/`PVRSRVCreateVirtualAppHint` both succeeded, yet `eglGetDisplay` still failed with `EGL_NO_DISPLAY`/0x3000.

Root cause, found via `strings` on the real vendor binaries (not guessed): **`libIMGEGL.suprx` and `libgpu_es4_ext.suprx` have `app0:module/libpvrPSP2_WSEGL.suprx`, `app0:module/libGLESv1_CM.suprx`, `app0:module/libGLESv2.suprx` hardcoded internally** (singular `module/`) as their own dependency-loading paths — this is Sony/Imagination's own compiled-in fallback, independent of whatever we pass via `PVRSRV_PSP2_APPHINT`. None of the 6 `.suprx` files contain the string `modules/` (plural) anywhere. So even though *our* explicit `sceKernelLoadStartModule` calls and AppHint pointed at wherever we told them to, `libIMGEGL`'s own internal load of its `WSEGL`/`GLESv1_CM`/`GLESv2` dependencies during `eglGetDisplay()` only ever looks at `app0:module/...` — if the files aren't there, that internal load silently fails and `eglGetDisplay` returns `EGL_NO_DISPLAY` with the misleading `EGL_SUCCESS` (0x3000) code.

**Fix (2026-07-18, second pass): renamed `modules/` back to `module/` (singular)** and reverted all three references. Rebuilt and verified via a real `cmake && make` + VPK packaging pass that `build/dungeon_hunter_2.vpk` now contains `module/*.suprx` (singular). **This is the one and only correct name for this directory — do not rename it again without re-checking the `strings` output above; the vendor driver's own hardcoded path is not something we can change.**

**Tested on hardware 2026-07-18 with `module/` (singular) — same `eglGetDisplay` 0x3000 failure, meaning the directory name wasn't the whole story.** Root-caused via `GrapheneCt/PVR_PSP2` (the actual open-source driver project this whole `.suprx` set comes from, https://github.com/GrapheneCt/PVR_PSP2): its own reference test (`unittests/gles1test1/gles1test1.c`) only declares **`"app0:libgpu_es4_ext.suprx"` and `"app0:libIMGEGL.suprx"`** as auto-loaded dependencies (via `SCE_USER_MODULE_LIST`, at `app0:` **root**, no subfolder) — it does **not** preload `libpvrPSP2_WSEGL.suprx`/`libGLESv1_CM.suprx`/`libGLESv2.suprx` at all. Those three are loaded **lazily by `libIMGEGL.suprx` itself**, internally, the first time `eglGetDisplay`/`eglInitialize` need them, using the paths already compiled into `PVRSRVInitializeAppHint()`'s own defaults (confirmed by reading `pvr_apphint.c` in that repo: the defaults already are `"app0:module/libGLESv1_CM.suprx"` etc. — matching what `glutil.c` was setting explicitly anyway, redundant but harmless).

**The actual bug:** `main.c` was explicitly pre-loading all 5 modules via `sceKernelLoadStartModule` before calling `gl_init()`. When `libIMGEGL` then tries to load its own dependencies internally during `eglGetDisplay`, it finds a same-named module already loaded (from our pre-load) — and its internal loader (`PVRSRVLoadLibrary`/`LoadNamedWSModule`, per that repo's source) has **no "already loaded, proceed anyway" handling**: any non-success there is treated as fatal, and `eglGetDisplay` surfaces it as `EGL_NO_DISPLAY` with the misleading `EGL_SUCCESS` (0x3000) code — regardless of directory naming.

**Fix (2026-07-18, third pass):** `main.c`'s `pvr_modules[]` now only preloads `libgpu_es4_ext.suprx` and `libIMGEGL.suprx`, from `app0:` root (no subfolder) — matching the reference test exactly. `CMakeLists.txt` packages those two at VPK root; `libGLESv2.suprx`/`libGLESv1_CM.suprx`/`libpvrPSP2_WSEGL.suprx` stay under `module/` (matching the driver's own compiled-in default, which `glutil.c` still sets explicitly — no change needed there). Verified via a real `cmake && make` + VPK pass that `build/dungeon_hunter_2.vpk` now has `libgpu_es4_ext.suprx`/`libIMGEGL.suprx` at root and the other three under `module/`.

**✅ CONFIRMED FIXED on real hardware 2026-07-18 (`log_057.txt`).** With only `libgpu_es4_ext.suprx`/`libIMGEGL.suprx` preloaded (from `app0:` root): both `LoadStartModule` calls succeed, `PVRSRVInitializeAppHint`/`PVRSRVCreateVirtualAppHint` succeed, **`eglGetDisplay -> 0x1 (err 0x3000)`** — this time `0x3000` is genuinely `EGL_SUCCESS`, not a failure (compare to every prior log where the display was `0x0`/`EGL_NO_DISPLAY`). `PVR_PSP2 EGL context created` and `PVR_PSP2 initialized` both print. The engine reaches `app Init is OK` and **the main loop runs for 960+ frames** with `GL pipeline clean (no error)` on almost every frame, correctly processing touch (`GameGLSurfaceView.nativeOnTouch`) and key input (`nativeKeyDown`/`Up`), reaching menu logic (`lastOpenMenuID`). This is the furthest the port has ever gotten — PVR_PSP2 bring-up (Phase 5's real blocker for the last 3 days) is done.

**New, different symptom reported by the user:** the screen shows a flat **aquamarine/turquoise color** instead of the game's actual UI — i.e. we've moved from "black screen, nothing draws" (the old vitaGL+Cg symptom) to "something draws (a clear color, most likely), but textured/shaded content doesn't appear on top of it." Frame 1 alone logs one `glGetError() = 0x0502` (`GL_INVALID_OPERATION`) — transient, doesn't recur on later frames, but worth keeping an eye on as a clue about what the first draw call that fails is.

- [x] **✅ Confirmed on real hardware 2026-07-18 (`log_058.txt`): NO shader compile/link failures.** With `glCompileShader_soloader`/`glLinkProgram_soloader` wired in (see below), a full run through the main loop (840+ frames) produced **zero** `glCompileShader(...) FAILED`/`glLinkProgram(...) FAILED` lines. **This definitively closes the original GLSL→Cg translation problem this session started from**: `shaders.pak`'s real GLSL ES source compiles and links cleanly against the real PVR_PSP2 GLSL ES compiler, end to end, with no translation of any kind. The remaining "aquamarine screen" issue is a content/asset problem, not a shader-language problem.
- [x] **Found and fixed 2026-07-18: several real GLES2 functions were stubbed as harmless-looking no-ops (`ret0`), a leftover from the old `vitaGL` era where they genuinely weren't implemented.** Now that we link the real driver (`libGLESv2_stub.a`), these all exist for real and were re-wired in `source/dynlib.c`: **`glCompressedTexSubImage2D`** (the standout suspect — its sibling `glCompressedTexImage2D` was already wired to the real function two lines above it in the same table, `glCompressedTexSubImage2D` was not; if the engine's textures are compressed, sub-image uploads were being silently swallowed, which would produce exactly "background renders, sprites don't" — aquamarine clear color with no texture content on top), plus `glBlendColor`, `glDetachShader`, `glValidateProgram` (lower-risk but equally real and available, fixed for consistency). Verified via `nm` that all four symbols exist in `libGLESv2_stub.a`, and via a real `cmake && make` build that linking still succeeds.
- [x] **Tested on hardware 2026-07-18 (`log_059.txt`): still zero shader compile/link failures, screen still aquamarine.** Confirms twice now that shader compilation is not the cause — the re-wired `glCompressedTexSubImage2D`/etc. didn't visibly change the symptom either. **Still no texture-file `fopen()` calls appear anywhere in ~840 frames of log**, which is the most telling fact: the engine hasn't even tried to load a texture/model asset yet — this isn't (only) a texture-upload bug, something upstream of asset loading is stuck.
- [x] **Architecture discovery 2026-07-18: DH2's UI is rendered via an embedded Flash player (GameSWF), not hand-coded native widgets.** Found while tracing `loadMovie`/`isMediaPlaying` in `out_ghidra.c`: a red herring at first (a `"loadMovie"`/`"unloadMovie"` string pair near `sprite_loadmovie`/`sprite_unloadmovie`) turned out to be **GameSWF's own ActionScript `MovieClip.loadMovie()` binding**, unrelated to `GLMediaPlayer`'s video playback — but it confirms the engine embeds **GameSWF, an open-source Flash/SWF player**, and renders menus as Flash movie clips (matches `gameswf_effects.bdae`, which `fopen()`s successfully in every log). **This means the high-level menu/flow logic likely isn't traceable in Ghidra's C pseudocode at all** — it's driven by SWF ActionScript bytecode and/or the embedded Python layer (`data/pydata/*.bin` — `engine_core_pycst.bin`, `scripts_pycst.bin`, etc. are generically-named Python constant/bytecode caches). Static analysis of *what specific state the game is stuck in* has likely hit its ceiling here — the fastest remaining path is hardware experimentation, not more Ghidra reading.
- [x] **`GLMediaPlayer_isMediaPlaying`/`GLMediaPlayer_loadMovie` real semantics checked and ruled out as the blocker:** `nativeLoadMovie` (`out_ghidra.c:410534`) is a thin pass-through to Java's `loadMovie`, and the real `GLMediaPlayer.java.loadMovie()` fires an Android `Intent` to a separate video-playing Activity and returns success immediately (fire-and-forget, doesn't block). Our `GLMediaPlayer_loadMovie` stub already mirrors that (fakes success immediately) and `GLMediaPlayer_isMediaPlaying` already returns `0` (not playing) — so the engine isn't stuck waiting on a "still playing" poll. Checked against the actual decompiled logic, not left as an assumption.
- [x] **Found and fixed 2026-07-18: missing font asset.** The engine (via FreeType, likely GameSWF's own text rendering) requests `/system/fonts/droidsans.ttf` and `#/system/fonts/droidsans.ttf` — real Android paths that obviously don't exist on Vita — and got `fopen() == 0x0` in every log so far. No font was ever staged in the repo (`extras/fonts/` was referenced by `deploy_and_launch_vita3k.sh` but never actually populated). Fixed: copied a real, freely-redistributable `DejaVuSans.ttf` (Bitstream Vera/DejaVu license — not derived from Gameloft's assets, safe to commit) into `extras/fonts/DejaVuSans.ttf`, packaged it into the VPK root via `CMakeLists.txt`, and added a redirect in `source/reimpl/io.c`'s `fopen_soloader` for both exact path variants. Verified via a real `cmake && make` + VPK pass that `DejaVuSans.ttf` is now packaged. **Not yet tested on hardware.** If GameSWF's UI rendering was silently bailing out on the font-load failure (plausible, unconfirmed), this could be what's blocking the Flash-driven menu from drawing anything beyond its background clear color.
- [x] **Root cause of the aquamarine screen found and fixed 2026-07-22 (static analysis, not yet hardware-tested).** Traced via `out_ghidra.c`: `GSInit::Update` (the boot/loading "game state") is a step machine; step 6 waits for the global `videoDone` (real `.bss` symbol, confirmed via `nm -D`) to become `1` before advancing to step 7 (background), 12 (`MenuManager::Init()`), 14 (`GetMenuByName("menu_splash")`) — that step has no fallback and spins forever if `videoDone` stays 0, which is exactly why `MenuManager`/`GSFlashMenu` never activate and no texture/SWF asset is ever requested (fully explains `lastOpenMenuID` staying 0). On real Android, `videoDone` is only ever set by `appResume()` (and only when `isDevicePause` was set) — i.e. the real "video finished" signal is the Activity pause/resume cycle triggered by launching the video-playing Activity, not an explicit movie-finished callback. Our `GLMediaPlayer_loadMovie` stub (`source/java.c`) faked immediate success but never touched `videoDone` or the pause/resume cycle, so the flag never flipped. **Fix:** `GLMediaPlayer_loadMovie` now resolves the real `videoDone` symbol via `so_symbol(&so_mod, "videoDone")` and writes `1` directly, right after faking the skip — smaller and more upstream than simulating a full `appPause`/`appResume` cycle. Also removed a half-finished diagnostic hack from `main.c` (a forced `MenuManager::PushMenu("menu_MainMenu")` call) that a prior session had left in uncompiled/untested — confirmed via the pseudo-C that `PushMenu` is only ever invoked from GameSWF ActionScript through the `NativePushMenu` native binding, never directly from C++ boot code, so that hack would have been a bypass rather than the real fix even if it had worked. Verified with a real `cmake && make` build (clean, no new warnings) — **not yet tested on hardware**, the test console (`192.168.3.15`) didn't respond to ping this session.
- [x] **`videoDone` fix CONFIRMED on real hardware 2026-07-22 (`log_063.txt`).** The engine advances past `GSInit::Update` step 6 for real: `LoadingStep_LoadBackground` appears, real texture `fopen()`s (`splash_final.tga`, `menugraphics0*.tga`), real sound `fopen()`s (all open/close cleanly, no hang), and all three menu SWFs (`dqshared.swf`, `dqmenus.swf`, `dqhud.swf`) load with their textures/fonts. The aquamarine-screen blocker is resolved.
- [ ] **New symptom found in the same log:** activity stops cold right after the second `dqhud.swf` open/close (no crash, no error) — several real minutes with no further progress on a loading/splash screen (no longer aquamarine). **Sound ruled out as the cause** (user's hypothesis, investigated and disproved): `VoxSoundManager::Initialize()` (`GSInit::Update` steps 8/9) is fully synchronous in this boot path — no thread, no semaphore/flag wait; all `GLMediaPlayer`/`Musicplayer` JNI audio stubs were already safe no-ops. A network-wait theory was also ruled out: `Application::IsInternetAccessEnabled()` → `nativeIsWifiAlive()`, already registered returning `0`, so the only network-touching path (`CXPlayerManager::Init()`) is never reached. **Most likely explanation (not fully confirmed):** `MenuManager::Init()` (`GSInit::Update` step 12) is its own multi-frame step machine (loads SWFs one at a time, inits HUD managers, scans Flash animations via `FlashAnimManager::ScanForAnims`, loads more PyData, constructs menu singletons) — legitimately slow chunked loading on real hardware, not necessarily a hang. Cases 6/8/10+ of that inner step machine were not fully traced — if a future log shows the exact same stopping point after waiting even longer, treat that as evidence of a real block and dig into those specific cases next (same methodology as the `videoDone` bug: find the step's real gating condition in `out_ghidra.c`, compare against what our stubs actually do).
- [x] **UX fix applied 2026-07-22, kept:** the loading-screen clear color is no longer the engine's own aquamarine — `glClearColor_soloader` (`source/utils/glutil.c`/`.h`, wired in `source/dynlib.c` same as the existing shader wrappers) forces black regardless of what the engine requests. Not yet hardware-tested.
- [x] **UX change attempted and REVERTED 2026-07-22 — regression found on real hardware (`log_064.txt`).** `GLMediaPlayer_loadMovie` briefly showed a blocking native `SceMsgDialog` ("Press X to continue") before unblocking `videoDone`. This hung the port forever, invisibly, right where the dialog wait began. **Root cause:** the `init_msg_dialog()`/`get_msg_dialog_result()` pattern in `source/utils/dialog.c` (also used by `fatal_error()`) is a leftover from the project's original vitaGL-based template — vitaGL's `vglSwapBuffers()` used to pump the `SceCommonDialog` compositor automatically as a side effect of its own swap call. Since the PVR_PSP2/EGL migration (see Phase 5 above), `gl_swap()` is just `eglSwapBuffers()`, and **nothing in this codebase calls `sceCommonDialogUpdate()`** (confirmed via grep) — without that, `SceMsgDialog` never composites or processes input, so waiting on `sceMsgDialogGetStatus()` blocks forever. Reverted `GLMediaPlayer_loadMovie` back to the log_063-confirmed behavior (log + write `videoDone=1` immediately, no UI gate). **`fatal_error()` is presumably equally broken and unverified** — treat any future use of `init_msg_dialog`/`get_msg_dialog_result` in this project as untested until `sceCommonDialogUpdate()` is properly wired (needs the real back-buffer address/pitch/sync object each frame, which PVR_PSP2/EGL doesn't expose as simply as vitaGL did — a real task on its own, not a quick add).
- [x] **`MenuManager::Init()` DOES finish — confirmed on real hardware 2026-07-22 (`log_066.txt`).** The title/splash screen shows (`Last PushMenu:: menu_splash`), touch input reaches the main menu (`GoToMainMenu`, `Last PushMenu:: menu_MainMenu`), the savegame slot loads correctly (`PlayerName = wolf`, warrior, level 1), and the game starts loading the player's 3D character model (`prince_modular.bdae` + 6 texture atlases) — all of this works. This is the furthest the port has ever gotten; the aquamarine-screen saga (Phase 5's blocker) is fully resolved.
- [x] **New crash found and root-caused via `so-crash-triage` (log + `.psp2dmp` + real `.so`, not guessed), fix applied 2026-07-22 (not yet hardware-tested).** Right after the character model's textures load once successfully, the engine re-requests the same `prince_modular.bdae` file but with `DATA_PATH` duplicated in the path (`ux0:data/dungeon-hunter-2/ux0:data/dungeon-hunter-2/data/3d/characters/prince/prince_modular.bdae`) — that `fopen()` fails, and the crash dump (parsed via `porting_tools/parse_dump.py` → `vita-parse-core`) shows a Data Abort with `PC` in `glitch::collada::COnDemandReader::read(int,int,void*) + 0x18` and `R0 == 0`. Real ARM disassembly (NOT Thumb — `-M force-thumb` gives garbage for this function) confirms: `ldr ip, [r0, #4]` loads the reader's internal stream member (NULL, because the doubled-path `fopen()` failed), then `ldr ip, [ip]` dereferences that NULL to read its vtable — crash. This is the engine's own on-demand asset streaming (`COnDemandReader`, distinct from the regular full-file `.bdae` texture loads that already worked earlier in the same log) re-resolving an already-absolute path through its own path-building logic a second time. **Fix:** `fopen_soloader` (`source/reimpl/io.c`) now detects a filename starting with `DATA_PATH` repeated twice and collapses it to one copy before proceeding — same style as the existing `droidsans.ttf` redirect in the same function, no engine logic touched.
- [x] **Real gameplay confirmed on hardware 2026-07-22 (`log_067.txt`).** The `DATA_PATH`-doubling fix worked — title screen, main menu, character load, and actual gameplay all reachable. Four new issues surfaced in the same log, all investigated:
  - [x] **Language defaulting to Russian — fixed (not yet hw-tested).** Root cause: a stale `dh2_settings.savegame` on the test device persisted a wrong language index from before `Get_PhoneLanguage` was correctly wired (that stub already returns 0/English correctly, confirmed against `DungeonHunter2.java`). Fix in `source/main.c`: force `SavegameManager::setLanguage(0)` every frame for the first 180 frames (via `Singleton<Application>::s_inst + offset 76` → `SavegameManager*`, real symbols) — done every-frame-in-a-window rather than at one guessed frame, same lesson as the `videoDone` fix (GSInit's settings-load timing isn't a fixed frame number).
  - [x] **Physical Vita buttons not working in gameplay — confirmed NOT a porting bug.** `appKeyPressed`/`appKeyReleased` in the real `.so` are debug-log-only stubs that never read the keycode, on real Android either. DH2 is a 100%-touch mobile game (all input goes through `nativeOnTouch` → `appOnTouch`); there is no native gameplay keycode mapping to port. Don't chase this further.
  - [x] **Visual distortion — root cause CONFIRMED, fix applied (not yet hw-tested).** Initial lead: the menu/HUD `.swf` files (`dqhud.swf`/`dqmenus.swf`/`dqshared.swf`/`dqcharmenu.swf`) all have a hardcoded 1024×768 (4:3) stage. The deeper, more fundamental root cause: `appInit()` (`out_ghidra.c:409955-409989`) picks a hardcoded logical rendering-canvas size purely from the screen width we report — for our 960-wide bucket that's **960×640** (`g_windowDimensions3`, confirmed local/`.symtab`-only, not in `.dynsym`, so not reachable via `so_symbol()`), and the entire rendering device (2D UI and 3D world alike) is created at that size via `glitch::createDevice(1, &g_windowDimensions, ...)`. The Vita's real surface is 960×544 — 96px short of what the engine assumes, causing the squash/stretch. **Fix:** new `glViewport_soloader` wrapper (`source/utils/glutil.c`/`.h`, wired in `source/dynlib.c` same as the existing shader/clear-color wrappers) remaps every `glViewport` call from the assumed 960×640 logical space into a centered, aspect-correct pillarboxed rectangle within the real 960×544 screen (scale 544/640=0.85, 816px-wide centered viewport, 72px black bars each side). Verified with a real `cmake && make` build — clean. **Not yet tested on hardware** — need a real screenshot/log to confirm the stretch is gone and the expected black side-bars appear.
  - [x] **Slow resource loading — cause identified, deliberately not changed.** Every log line (including hundreds of successful Debug-build `fopen()`/`fclose()` traces during loading) does a synchronous `fflush()` (`source/utils/logger.c:110`) — a plausible real contributor to load time. User's explicit choice: leave as-is while the port is still in active debugging, since immediate-flush logging is exactly what made both crash triages this session (`videoDone` hang, `DATA_PATH`-doubling crash) precisely diagnosable from the log alone. Revisit once the port is more stable.
- [x] **Distortion fix CONFIRMED working by the user (`log_068.txt`, 2026-07-22).** The `glViewport_soloader` letterbox resolved the general screen distortion. Menu navigation now works well too (`lastOpenMenuID` progresses through `menu_CharacterMenu`, `CharacterSheetNew/Stats`, `MapSheet`, `QuestLogSheetNEW`, `FaerySheet`, `SkillTreeSheetNew`, `InventorySheetMain`, back-button working).
- [x] **New symptom: 3D models render as flat aqua, some menu screens show no character animation — root cause CONFIRMED, partially fixed.** Investigated concretely (not guessed): several textures are genuinely absent from every data source we have access to — checked the raw `.apk` (0 `.tga` files at all — this game doesn't bundle 3D assets in the APK, it downloads/caches them progressively as played), `Dungeon-Hunter-2-HD-v1-0-2-cache.zip` (full installed-app-data dump, 242 `.tga` — same count as our already-extracted folder, nothing new), and the extracted folder itself. **Confirmed missing everywhere:** `tex_prince.tga`, `tex_princehair.tga`, `tex_plate_shoulder_default_000.tga`, `tex_prince_pants.tga` (character skin/hair/armor, likely used for character-sheet portraits, not the "modular" 3D world model which loads fine), `char_faerie.tga` (matches the blank `FaerySheet` menu), `fx_spark.tga`. Likely explanation: the captured app-data dump (dated 2024-04-18) came from a device that hadn't played far enough to trigger these specific assets' download/cache.
  - [x] **One case fixed in code:** `env_swamp_alpha.tga` (and, by the same pattern, likely every other dungeon theme's environment alpha mask) only exists on disk with a `pvr2_` prefix (`pvr2_env_swamp_alpha.tga`) — confirmed across all 17 theme alpha masks in the dump except `darkwoods` (which has both). The engine always requests the plain name. Fixed in `source/reimpl/io.c`'s `fopen_soloader`: on a failed open of `.../textures/env_*_alpha.tga`, retry with a `pvr2_` prefix inserted before reopening — fixes this for every dungeon theme, not just swamp.
  - [ ] **Not fixable in code — genuine data gap.** `tex_prince.tga` and the others above aren't recoverable from any source we have. Needs a user decision: (a) obtain a more complete data dump from a device/emulator that's played further into the game, (b) supply placeholder/substitute textures as a cosmetic stopgap, or (c) leave as a documented known gap for now.
  - [x] **2026-07-23 follow-up investigation (both leads the user asked about, both closed with concrete evidence, not guesses):**
    - **`shaders.pak` re-extracted and listed in full — confirmed it contains zero textures.** All 34 entries are `.glsl` shader source plus two empty `cg.config`/`glsl.config` markers (`unzip -l` output); there is no `.tga` of any kind inside it. This was never going to recover the missing character textures — closing that lead. Separately confirmed (via `out_ghidra.c:30298`, `Application::InitWin32`) that the engine opens `"shaders.pak"` itself as a whole file through one native call (an `IDevice`-vtable "load shader pack" method, not the `GLResLoader` Java asset bridge) and unzips it internally — consistent with shaders already compiling with zero failures in `log_058`/`log_059` before this session, so the archive is evidently already reaching the device somehow. **Not confirmed:** neither `CMakeLists.txt` nor `porting_tools/manage_vita.py` stage `shaders.pak` explicitly (no reference to it in either file) — it must be reaching `ux0:data/dungeon-hunter-2/` via a manual bulk copy of the app's `files/` dump rather than our own tooling. Low priority to formalize now since it's already working.
    - **Investigated whether the missing character textures are dynamically generated/composited at runtime — confirmed they are not.** Searched `out_ghidra.c` for any offscreen-rendering/compositing path that could synthesize a character-skin texture (`RenderTarget`, `DynamicTexture`, `GenerateTexture`, `CompositeTexture`, `Customiz*`, `portrait`): the only hit is the generic engine-level `glitch::video::IRenderTarget` class (push/pop render target, a normal offscreen-rendering primitive available to any 3D engine) — zero evidence it's ever used for character texture generation. Also grepped every `data/pydata/*_pycst.bin` compiled-Python-constant file (`character_properties`, `character_classes`, `faeries`, `design`, etc.) for any `.tga` string at all — **zero matches in any of them**, confirming texture bindings are not script-driven either. Instead, traced the missing filenames to their actual static references, the same mechanism used by every texture that already loads fine (e.g. `atlas_modular_warrior.tga` in the working `prince_modular.bdae`): `tex_princehair.tga`/`tex_prince_pants.tga`/`tex_plate_shoulder_default_000.tga` are hardcoded inside `data/3d/characters/npcs/animations/cs_swamp_intro_prisonner_scene01.bdae` (the imprisoned-Prince NPC model used in the swamp intro cutscene, not the playable character), and `char_faerie.tga`/`fx_spark.tga` are hardcoded inside `data/3d/characters/faeries/animations/faeries_template_anim.bdae` (the Faerie companion). Also checked the real, active `dqcharmenu.swf` (10 MB, zlib/`CWS`-compressed — decompressed with `zlib` to search it): its own texture references are a completely separate, already-present atlas set (`MenuGraphics01-06.tga`, `MenusGraphics.tga`, `map_bottom.tga`, `splash_final.tga`) with **no overlap** with the missing names, so the 2D character-sheet menu chrome itself isn't at risk here — whatever the user is currently seeing as flat aquamarine is the 3D cutscene-NPC or Faerie model layer, not the UI. **Net result: no dynamic-generation path exists to exploit instead — these remain genuine missing static asset files**, exactly as the entry above already concluded; this pass only closes off the two open questions (shaders.pak, runtime generation) rather than changing the conclusion. The (a)/(b)/(c) user decision above is still the only way forward. One loose end not worth chasing further: the bare `tex_prince.tga` (no hair/pants/shoulder suffix) doesn't turn up referenced anywhere text-searchable (`.bdae`, pydata, or the decompressed active menu SWF) — it may not actually be a live dependency of anything currently reachable in this dump.
- [ ] **Next action:** install the rebuilt VPK (`build/dungeon_hunter_2.vpk`, 2026-07-22 — has the `pvr2_` redirect) and see whether the swamp environment alpha mask now loads and whether that changes anything visually. The still-missing character/faerie textures still need the (a)/(b)/(c) decision above before they can be addressed — confirmed 2026-07-23 there is no code-side fix available (see follow-up investigation above).
  - [x] **2026-07-23, second follow-up — real root cause of the "qata" path found via `log_067.txt`/`log_068.txt`, and a cosmetic-substitute fix implemented.** The user noticed the engine logs a second, different failed path for these same textures: `ux0:data/dungeon-hunter-2/qata/3d/textures/tex_prince.tga` (i.e. `data` → `qata`, only the leading letter changes). Investigated concretely, not guessed: `grep -ac qata` on the real `libDungeonHunter2.so` returns **zero** — the string `"qata"` does not exist anywhere in the binary, so it isn't a hardcoded alternate content-pack path; it must be constructed at runtime. Traced the actual log sequence carefully: after the first `fopen()` on the plain `data/...` path fails, the engine (not our own `io.c` — grepped, we have no such logic) walks *up* the directory tree opening each ancestor (`.../data/3d/textures`, `.../data/3d`, `.../data`, `.../dungeon-hunter-2`, `ux0:data` twice) — all of which fail too, since `fopen(dir, "rb")` can't open a directory — then retries the *entire* path a second time with `qata` substituted for `data`, and gives up. This is a generic, always-executed fallback in the engine's on-demand asset reader (likely `glitch::collada::COnDemandReader`, the same subsystem behind the earlier `DATA_PATH`-doubling crash), **confirmed to be 100% original-engine behavior — nothing in this port's code touches or produces this string.** Searched a second, independently-downloaded, different-SKU app-data dump (`com.gameloft.android.GAND.GloftD2HP`, v1.0.5, package ID different from the original `GloftD2SS` dump) for a literal `qata` folder or any of the missing filenames — **found neither** (same 240ish `.tga` count, same gap). Two independent real-Android dumps now agree: this content doesn't exist in any install we can access, and the `qata` fallback is most likely dead/legacy code that would fail identically on real hardware too — not something specific to the port.
    - **Fix applied (`source/reimpl/io.c`, `fopen_soloader`):** since the real Gameloft art for `tex_prince.tga`/`tex_princehair.tga`/`tex_prince_pants.tga`/`tex_plate_shoulder_default_000.tga` (used only by the one-time swamp-intro cutscene NPC, `cs_swamp_intro_prisonner_scene01.bdae`) and `char_faerie.tga`/`fx_spark.tga` (used by the Faerie companion, `faeries_template_anim.bdae`) is unobtainable from any source available, added a targeted redirect — same style as the existing `pvr2_`/`DejaVuSans.ttf` redirects — that opens the closest **real, already-legitimate in-game texture** instead of failing outright: the four Prince-cutscene textures redirect to `prince-warrior.tga` (the closest real Prince skin atlas that already ships and loads), `fx_spark.tga` redirects to `fx_spark_01.tga` (near-identical name, likely just renamed between versions), and `char_faerie.tga` redirects to `fx_sparkles_01.tga` (no dedicated Faerie body texture exists anywhere in either dump; this is the least-bad available substitute). **This is a cosmetic stopgap, not a real fix** — UV layouts won't match, so the cutscene NPC/Faerie will look textured-but-wrong rather than accurately skinned; it trades the "flat aquamarine, clearly broken" look for "textured, slightly off" for a one-time cutscene and a companion model, neither of which is core gameplay. Verified with a real `cmake && make` build (VITASDK/PATH set, full rsync-to-`/tmp` flow) — compiles clean, `build/dungeon_hunter_2.vpk` repackaged. **Not yet tested on hardware.**
  - [ ] **2026-07-23, third follow-up — `log_070.txt` shows this is a much bigger, systemic problem than missing textures: the ENTIRE 3D world (not just a few models) fails to render, while 2D UI (menus, HUD, controls) draws correctly, in both menus and live gameplay.** Read `log_070.txt` in full (4185 lines, this is a build that predates today's `tex_prince.tga`-style redirects, so those specific failures are expected/unrelated noise). Concrete findings, not guesses:
    - **Gameplay logic is fully working under the hood.** The log shows real combat: touch input (`Toucxhhhhhhhhhhhhhhhh` debug spam with live coordinates), Prince combo-attack animations (`prince_1hand_combo_01/02`, `prince_1hand_pre_combo_*`), a `moth` enemy's full attack/idle/walk cycle, a fire projectile (`elemental_bolt_fire.bdae` + `fx_radial_white.tga`), a weapon-trail effect (`fx_weapon_trail_blue_gradual.tga`), and a hit-blood effect (`bloodsplat_hero.bdae`) — all their `.bdae`/`.tga` `fopen()` calls succeed. `[gl_diag]` reports "GL pipeline clean (no error)" on every sampled frame except one transient `0x0502` on frame 1. **This rules out an asset-loading or GL-error-level bug as the cause** — the engine believes it's drawing a fully-populated 3D scene every frame, with no driver-reported error.
    - **The `glClearColor_soloader` override (already in place, forces black unconditionally — see `glutil.c`) means the aquamarine the user still sees cannot be coming from any `glClear()` call anywhere in the engine.** Re-read the wrapper to confirm: it ignores the requested RGB unconditionally and always writes black. Since the symptom persists, the visible aquamarine must be **actual drawn content** (a quad, a background/skybox layer, or an incomplete-framebuffer's undefined contents being sampled) — not a clear-buffer color — even though it looks identical to the old "flat clear color" symptom to the eye.
    - **Leading hypothesis (matches the user's own guess, not yet confirmed on hardware): a depth-test/Z-buffer or render-target problem letting a background/environment layer always paint over the real 3D geometry**, rather than a shader or texture problem (shader compiles/links clean per Phase 5's earlier finding; most `.tga`/`.bdae` opens for this exact scene succeed per the log excerpt above). Checked what's already in place: EGL config requests `EGL_DEPTH_SIZE, 16` (`glutil.c`'s `gl_init()`), and `glEnable`/`glDisable`/`glDepthFunc`/`glDepthMask`/`glClear` are all wired straight to the real driver functions in `dynlib.c` (no port-side override to suspect there). This does **not** confirm the depth buffer is actually working end-to-end on real hardware — EGL can silently grant a 0-bit depth config if the driver doesn't like the request, independent of what we ask for.
    - **Instrumented (not yet hardware-tested) rather than guessed further:** added three concrete diagnostics so the next real-hardware log answers this directly instead of more speculation:
      1. `glutil.c`'s `gl_init()` now logs the *actual* `EGL_DEPTH_SIZE`/`EGL_STENCIL_SIZE` granted by `eglChooseConfig` (via `eglGetConfigAttrib`) right after config selection — confirms whether the 16-bit depth buffer we requested was actually honored, or silently dropped to 0.
      2. New `gl_log_render_diag()` (`glutil.c`/`.h`), called from `main.c`'s existing once-per-60-frames diagnostic block: logs `GL_DEPTH_BITS`, whether `GL_DEPTH_TEST` is currently enabled (`glIsEnabled`), and whether a non-default framebuffer is bound — if one is, also logs `glCheckFramebufferStatus()` so an incomplete render-target used for the 3D pass would show up explicitly instead of silently producing undefined/garbage pixel content.
      3. `glClearColor_soloader` now logs (`l_debug`) the RGBA the engine actually requested before forcing black — lets us confirm from a real log whether the engine keeps re-requesting the aquamarine-ish color every frame (e.g. for a render-target clear) even though it never reaches the screen, which would support the render-target hypothesis over a "background quad" one.
    - Verified with a real `cmake && make` build — compiles clean, `build/dungeon_hunter_2.vpk` repackaged (now has the depth/FBO/clear-color diagnostics on top of the two prior fixes in this session). **Not yet tested on hardware.**
    - **Next action:** install this VPK, reproduce (menu is enough — the earlier `log_068` and this `log_070` both show the issue present from the main menu onward, not just in dungeons), and pull a fresh log. The three new log lines (`EGL config: depth=...`, `[gl_diag] frame N: depth_bits=... depth_test=... bound_fbo=...`, and the `glClearColor requested (...)` lines) should make the next root-cause step a direct read instead of another round of hypothesis-forming.
- [x] **`log_071` root cause found: the VPK/eboot the user was testing was STALE, built before that session's source edits.** `dungeon_hunter_2.elf`/`eboot.bin` mtimes predated the just-edited `.c`/`.h` files — none of the three new diagnostic lines above appeared in the log because that code had never actually been compiled in. This is a process bug, not a rendering bug: **always check that `build/*.elf` is newer than the edited sources before trusting a "the fix didn't work" log.** Rebuilt for real (`porting_tools/build/build_and_install.sh`, rsync-to-`/tmp` + `cmake && make`) and confirmed via `stat` that the artifacts were fresh before shipping.
- [x] **3D world rendering CONFIRMED WORKING end-to-end, `log_076` (2026-07-23) — the aquamarine/black-screen saga (Phase 5) is closed.** With a genuinely fresh build, the depth-range/draw-call/cull/texture-binding diagnostics all came back healthy (thousands of draw calls per second, correct depth-test bracketing, real shader programs and textures bound) and the user confirmed the game is playable: menu, character load, dungeon combat, enemies, effects, all visible. The earlier `GL_DEPTH_RANGE` stuck at `(1.0, 1.0)` reading (`log_075`) turned out to be a **misread, not a real bug** — `glDepthRangef_soloader`'s call-args log (added specifically to check this) showed the engine only ever requests sane values (`0.0/0.8`, `0.0/1.0`); `glGetFloatv(GL_DEPTH_RANGE)` just wasn't reliable to sample at that instant on this driver. No single fix "solved" the aquamarine screen at the end — it was the cumulative effect of the `videoDone`/viewport-letterbox/`DATA_PATH`-doubling fixes from earlier in Phase 5, combined with finally testing a build that actually contained them.
- [x] **Extensive GL-state diagnostic instrumentation added during this investigation (`glutil.c`) is now gated behind `DEBUG_SOLOADER`, not just its `l_info`/`l_debug` log calls.** The rendering bug is closed, so the actual *work* behind `gl_log_render_diag`/`log_first_draw_state`/the `glUniformMatrix4fv` NaN-scan (dozens of `glGet*()` driver round-trips every ~60 frames, a 16-float scan on every single matrix upload) was pure overhead in Release builds even though the log macros themselves already compiled to no-ops — only the logging *macro* was gated before, not the computation feeding it. Fixed by wrapping the bodies in `#ifdef DEBUG_SOLOADER`, not just relying on the log macros.

### Phase 6 — Input — ⚠️ reopened 2026-07-24, physical-button mapping still unresolved
`GameGLSurfaceView.nativeOnTouch` is a single touch entry point (unlike Zenonia's 3-vs-4-int
`handleCletEvent` split) — confirm its real signature/argument packing from the pseudo-C before
wiring `sceTouchPeek`. `nativeKeyDown`/`nativeKeyUp`/`nativeonTrackballEvent` cover physical
buttons/d-pad; `nativeAccelerometer` and `nativeSetOrientation` are extra surfaces the Zenonia ports
didn't have — decide up front whether accelerometer input is simulated (fixed neutral value) or
mapped to something on Vita, and document the choice.

**Outcome (2026-07-13):** Confirmed `nativeOnTouch` signature requires JNI `env` and `clazz` (like all JNI calls). Fixed a massive bug in `main.c` where all `native*` C function pointers lacked `JNIEnv*` and `jobject` arguments, which would have passed garbage to the native registers. Re-wired `sceTouchPeek` passing correct scaled touch arguments. `nativeAccelerometer` and `nativeSetOrientation` will be intentionally ignored/unimplemented, simulating a fixed device orientation without tilt input, as DH2 doesn't use tilt mechanics for gameplay.

**`appKeyPressed` confirmed a no-op stub — TWICE now, do not re-investigate a third time without new evidence.** First found 2026-07-22 (`log_067.txt`, see Phase 5 above): "`appKeyPressed`/`appKeyReleased` in the real `.so` are debug-log-only stubs that never read the keycode... there is no native gameplay keycode mapping to port." **Independently re-confirmed 2026-07-24** by tracing `Java_..._DungeonHunter2_nativeKeyDown` all the way through `out_ghidra.c`: it calls `appKeyPressed(void)`, whose entire body is `_DEBUG_OUT("keypresseddddddddddddddddddddddddd %d", lastOpenMenuID); return;` — it doesn't even reference its keycode argument. **No integer sent through `nativeKeyDown`/`nativeKeyUp` can ever drive gameplay or movement in this compiled `.so` — not D-Pad codes, not standard Android gamepad codes (`KEYCODE_BUTTON_A/X/Y/L1/R1` = 96/99/100/102/103, which DO work via the same entry point on the sibling Prince-of-Persia port's different engine/build — tried here 2026-07-24 by analogy, reverted once this trace confirmed it can't work). The D-Pad/Back/Menu keycodes already sent for menu navigation (`btn_map` in `main.c`) still have a real effect, but through a **different** native function (`nativegetState`, called directly by the real Java `onKeyUp` for MENU/BACK specifically) — not through `appKeyPressed`. **The only real gameplay input path this engine exposes is `appOnTouch` (fully implemented, dispatches to real vtable methods) — i.e. `nativeOnTouch`.** Any future physical-button work (combat actions, movement) must go through synthesized touch, never keycodes.

**Also reverted 2026-07-24 (see above): forcing the `"DPad"` saved option on.** `Application::IsUsingDPad()`'s readback did confirm the write took (`log_077`: `IsUsingDPad() readback after forcing DPad=1: 1`), so this wasn't a "the option didn't stick" failure — but since `appKeyPressed` is dead, there was never going to be a working D-Pad movement path for it to enable. Worse, `Character::Ctrl_Click` (`out_ghidra.c`) skips its own click-to-move/auto-target logic specifically *when* `IsUsingDPad()` is true, assuming a real D-Pad is handling movement instead — forcing this option on with no functional replacement risked silently breaking whatever touch-based movement/targeting already worked, for zero benefit. Reverted to not touching this option at all.

**Combat action buttons (Cross/Square/Triangle/L1/R1) — partially working, coordinates calibrated against a real Vita screenshot, root cause of the non-working ones still open.** Physical buttons are wired in `main.c` as synthetic `nativeOnTouch` taps at each HUD icon's on-screen coordinate (`action_btn_map`), each with its own fixed `pointer_id` (1-5) so they never collide with the real touch panel (`pointer_id` 0) or each other. Coordinates were originally guessed from an iPhone-4 reference screenshot (different aspect ratio, 960×640) and then **re-measured directly from a real Vita capture** (`2026-07-23-225855.jpg`, our actual letterboxed 960×544 output) by cropping/inspecting each icon's center — current values:
  - `SCE_CTRL_CROSS` → (180, 445), plain red sphere, bottom-left — **confirmed NOT working** (`log_083`/`log_084`: user reports no effect in gameplay).
  - `SCE_CTRL_SQUARE` → (683, 373), sword+flame icon — **confirmed NOT working**.
  - `SCE_CTRL_TRIANGLE` → (760, 453), sword+helmet icon (biggest, bottom-right corner) — **confirmed WORKING** (triggers what the user described as "the sword's power").
  - `SCE_CTRL_L1` → (810, 273), gold rune-wheel icon — unconfirmed (not clearly even a real skill slot; the user's savegame hadn't shown a second/third skill icon like the iPhone reference did, most likely because no skills are equipped yet on this character).
  - `SCE_CTRL_R1` → (820, 50), health-potion quick-use icon — unconfirmed, and not really a combat "power" even if it works; a stopgap mapping.

  Since Cross/Square/Triangle all use the *identical* code path (only the coordinate differs) and Triangle works, the bug is specific to those two coordinates or to what's actually at them — not the touch-synthesis mechanism itself. Two things NOT yet tried: (1) a real fingertip tap directly on the touchscreen at those same coordinates during gameplay, to tell "our synthetic coordinate is wrong" apart from "that HUD element isn't a real button" (the sibling Prince-of-Persia port's own notes describe exactly this ambiguity — a synthetic touch on Cross there was found to hijack menu list-navigation instead of ever registering as a real gameplay tap, because that port's loader has no signal to tell "in a menu" from "in gameplay" apart); (2) explicit logging of our own synthetic touch calls (added 2026-07-24, `action_btn:` log lines in `main.c`, not yet confirmed against a fresh log) to settle whether the calls are even reaching `nativeOnTouch` with the coordinates we think they are.

**2026-07-25/26: root cause of the coordinate mismatch found and fixed — CONFIRMED on hardware (`log_091.txt`).** The user reported having to tap noticeably *below and to the right* of a real on-screen HUD button (finger on the touchscreen, not a physical button) for it to register. Root cause: `glViewport_soloader` pillarboxes every render call from the engine's assumed 960×640 logical canvas into a centered 816×544 sub-rect of the real 960×544 screen (scale 0.85, x-offset 72px) — but the real-touch handling in `main.c` (`sceTouchPeek` path) and the `action_btn_map` synthetic-touch coordinates (both measured directly off physical screenshots, see above) were fed straight into `nativeOnTouch` with **no inverse letterbox applied**, so the engine hit-tested them against the wrong logical position. **Fix:** added `glutil_screen_touch_to_logical()` (`source/utils/glutil.c`/`.h`), inverting the exact same scale/offset math `glViewport_soloader` already uses, and applied it to both the real touchscreen path and `action_btn_map` in `main.c`. **Verified end-to-end on hardware, `log_091.txt`:** the engine's own `Toucxhhh` echo lines show the exact expected converted coordinates for every synthetic tap — e.g. physical `(180,445)` → logical `(127,523)`, `(683,373)` → `(718,438)`, `(760,453)` → `(809,532)` — an exact match to `glutil_screen_touch_to_logical`'s math, not just "no crash". This directly fixes the user-reported "must tap below the image" bug. **Not yet separately reconfirmed:** whether Cross/Square (previously "confirmed NOT working", line above) now actually register in gameplay, or whether this fix changes Triangle's previously-working status — the coordinate delivery is now provably correct, but the in-game *effect* of each button needs a fresh hands-on session to confirm.

**Separately reported the same session, not yet root-caused: enemies (confirmed troll, `log_083`) appear invisible in actual combat, while the player character, UI, and environment all render correctly.** Investigated at the GL-state level and came up empty — NOT a rendering-pipeline failure by any measure checked: the troll's `.bdae` model/animations all `fopen()` successfully (no `FAILED`), and a per-diagnostic-window "distinct textures used" counter (`glutil.c`, `MAX_TRACKED_TEXTURES=16`) showed up to 16 different texture IDs actively bound and drawn during combat, with new IDs appearing progressively as the fight goes on — consistent with enemy models genuinely being drawn with their own materials, not skipped. Depth test, blend mode, shader program, and viewport all sampled normal at the exact frames with peak texture diversity.

**2026-07-24 follow-up: the "16 distinct textures, consistent with the enemy being drawn" conclusion above was unverified, not confirmed.** `log_084` (another troll-combat session) showed the counter pegged at exactly 16/16 in *every* sampled window — but `track_seen_texture()` silently stopped recording once the array was full, so there was no way to tell "there really are only 16 distinct textures in this scene" apart from "there are 17+ and we dropped the rest (possibly including the troll's own)". Fixed in `source/utils/glutil.c`: raised `MAX_TRACKED_TEXTURES` 16→64 and added an overflow flag (`s_seen_textures_overflowed`) so a future cap-hit reports `distinct_textures_used=N+ (cap hit, some IDs dropped)` instead of a flat number indistinguishable from "that's everything". Verified with a real `cmake && make` build (Debug, `DEBUG_SOLOADER` on) — compiles clean. **Not yet tested on hardware** — the next hardware log's `[gl_diag_textures]` line should finally give a real answer to "does the troll's texture ever bind, or not".

Also investigated (same session) whether the pseudo-C (`out_ghidra.c`) shows a code-level reason enemies specifically could fail to render. Two findings: (1) **ruled out** — `BaseMeshSceneNode<CSkinnedMeshSceneNode>` (used by monsters like the troll) and `BaseMeshSceneNode<CModularSkinnedMeshSceneNode>` (used by the player's modular Prince model) both delegate to the identical `glitch::collada::CMeshSceneNode::render`/`onRegisterSceneNode` — mesh-type is not what differentiates player from enemy rendering. (2) **plausible, unconfirmed** — `CharProperties::PROPS_GetOpacity()` (`out_ghidra.c:157788`) reads a raw per-character int field at offset `0xb5c` and scales it by `1/256` into a 0.0–1.0 render alpha; if monster entries default this field to `0` while the player's gets set through a different (e.g. savegame-driven) path, the enemy would render fully transparent — zero GL errors, normal draw-call counts, exactly matching what's been observed so far. Could not confirm who actually calls this getter for enemies specifically (the call site is reached through a C++ vtable, opaque to text search in Ghidra's decompiled output) — this needs either a live memory read of that field for a troll instance, or a small patch logging the material alpha/blend color right before `drawMeshBuffer` in `CMeshSceneNode::render`, both bigger than a quick follow-up. **This is the current leading hypothesis** — next step once the texture-cap data comes back inconclusive (i.e. confirms the troll's texture *does* bind) is to instrument this specific opacity field instead of more GL-state dumping.

**2026-07-25: texture-cap fix paid off — `log_086.txt` (fresh hardware run, Bogwomp combat) confirms the enemy's textures DO bind and draw.** Combat windows now show 25-27 distinct textures (`gl_diag_textures`, frames 541-721), ~19 of them brand-new relative to the pre-combat baseline of 8 — i.e. real, previously-invisible draw activity, not truncated data. This kills "enemy never draws" and strengthens the alpha/opacity hypothesis above: draws happen, textures bind, `GL_BLEND` is on with standard `GL_SRC_ALPHA`/`GL_ONE_MINUS_SRC_ALPHA` factors (`gl_diag2`, same frames) — if the material alpha is 0, this is exactly what fully-transparent-but-still-drawing looks like. Root-cause confirmation (which function writes/fails to write the `CharProperties` opacity field for monster spawns vs. player init) is in progress.

**2026-07-25: separate bug found and fixed — repeating icon-list HUD panel bleeding extra rows onto screen (reported via a real Vita screenshot, `2026-07-25-004631.jpg`, compared against a real-Android reference screenshot showing a clean HUD with no such column).** Root cause: `source/dynlib.c` wired `glScissor` straight to the real driver function, unlike `glViewport` (already remapped by `glViewport_soloader` for the 960×640-logical-canvas-vs-960×544-real-screen letterbox, see the distortion fix above). The engine issues `glScissor` calls in that same assumed 960×640 logical space — almost certainly to clip a HUD icon-list sub-panel to its intended visible row count — but since the clip rect was never remapped, it no longer lines up with the real letterboxed content, letting rows that should be clipped off bleed through as a repeating icon column. **Fix:** added `glScissor_soloader` (`source/utils/glutil.c`/`.h`), reusing `glViewport_soloader`'s exact scale/offset math, wired into `source/dynlib.c` in place of the raw `glScissor` entry. Verified with a real `cmake && make` build — clean. **Not yet tested on hardware.**

**2026-07-26: REOPENED — hardware test (`log_091.txt`) rules out the `glScissor` hypothesis above.** Added logging to `glEnable_soloader`/`glDisable_soloader` (every real `GL_SCISSOR_TEST` toggle) and to `glScissor_soloader` (both the incoming logical rect and the remapped physical rect) to finally correlate this against the specific frames the repeating column is on screen, since the user reported the icon column was still present in the same screenshot bug (`2026-07-25-004631.jpg`) despite the fix above already being in that build. Result: `GL_SCISSOR_TEST` is toggled **exactly once, at frame 0** (`glDisable`, with a degenerate `(0,0,0,0)` rect) and never touched again for the rest of a 1180+ frame session. The engine never re-enables scissor test during HUD rendering at all in this session, which rules out a scissor-remap mismatch as the cause of the repeating column — the `glScissor_soloader` fix was real and correct for whatever bug it was originally built for, but it isn't what's producing *this* icon column. Root cause must be inside the Flash/GameSWF HUD renderer itself (`dqhud.swf`, "SHOW OF THE HUD" trace lines), most likely a list/repeater bound to a garbage or wrong count from a native binding — **not yet found**: `HUDControls::RefreshHUD()/Touch()/OnEvent()` (`out_ghidra.c:196459/196487/196520`) don't manipulate any icon count/array, and the actual native getter (if any) that feeds a buff/quickslot/skill-icon count to the SWF hasn't been located yet. Next step is tracing that binding, not more GL-state diagnostics.

**2026-07-26: root cause FOUND (static analysis only, console unreachable) — this is a HUD-layout-tier misselection, not a buff/quickslot count bug, and a fix has been coded (not yet hardware-tested).** The native binding isn't `NativeGetBuffs` (`out_ghidra.c:226280`, ruled out — properly bounded by the real `BuffInst` deque, returns a correctly-sized array). The actual mechanism is a **HUD-layout selector** read via `Application::GetSavedOption(char const*)` (`_ZN11Application14GetSavedOptionEPKc`, `out_ghidra.c:20949`) with the key `"HUDStyle"`, consumed by both `InfoHUDManager::initCachedChars()` (`out_ghidra.c:198837-198927`) and `HUDControls::initCachedChars()` (`out_ghidra.c:196749-196767`): `iVar1 < 2` selects a fixed 3-icon phone-style bar (`btn_skill1/2/3`); `iVar1 >= 2` selects a **scrollable icon-list carousel** (`HUDelements.controls.controls.list.btn_0`/`btn_pre0`/`btn_post0`) meant for tablets — that carousel widget is the actual "repeater" producing the repeating icon column in `2026-07-25-004631.jpg`.

Three-layer root cause, all confirmed directly in `out_ghidra.c`: (1) `Arrays::GameOptionTable` (`.bss`) is empty until populated at runtime by `Arrays::GameOptionTable::read()`, called from `PyDataArrays::Load()` during `GSInit::Update` case 3 — external Gameloft "PyData" asset. (2) `SavegameManager::_initSettings(this, false)` (`out_ghidra.c:247727-247779`) is the **only** place that inserts default option values (including `"HUDStyle"`) into the runtime map, and only `if (Arrays::GameOptionTable::size != 0)` — called from `GSInit::Update` case 4. `SavegameManager::__loadOptions()` only *updates* an existing map entry, never inserts a missing key, so a persisted `dh2_settings.savegame` can't rescue a key that was never registered as a default. (3) **The actual bug**: `Application::GetSavedOption()` (`out_ghidra.c:20949-20962`) does `this_00 = *(SavegameManager**)(this+0x4c); if (!SavegameManager::hasOption(this_00,key)) return;` — that `return;` on a miss leaves **whatever garbage was already in the return register (r0)**, unlike `getAutoReorientation()`/`getLanguage()`, which both have a proper hardcoded fallback for their own keys. `"HUDStyle"`/`"DPad"` have no such protection. If `"HUDStyle"` is missing from the options map the first time the HUD reads it (a PyData/options-table load-timing difference on this port vs. the reference device — same general class of boot-ordering issue as the `videoDone`/language-force fixes elsewhere in this doc), whatever garbage happens to be `>= 2` picks the carousel branch. This also reconciles with the already-confirmed hardware fact from the `glScissor` reopening above (`log_091`: `GL_SCISSOR_TEST` toggled once at frame 0, `glDisable`, never re-enabled) — the reference/fixed-bar path never needs scissor during gameplay (nothing to clip), while the carousel's row-clipping mask is exactly the scissor state that's stuck disabled, so every row renders unclipped and stacked down the screen.

**Fix implemented in `source/patch.c` (2026-07-26), same `hook_addr`/`SO_CONTINUE`/`so_symbol` mechanism already used for the license-check bypass:** hooks `Application::GetSavedOption` (`_ZN11Application14GetSavedOptionEPKc`); for the `"HUDStyle"` key specifically, if the real `SavegameManager::hasOption` (`_ZNK15SavegameManager9hasOptionEPKc`, via the same `this+0x4c` offset the real function itself uses) reports the option isn't registered, forces a safe return of `0` (fixed 3-icon phone-style bar) instead of leaving garbage in the register. Every other key (`"DPad"`, volume, etc.) is untouched — only `"HUDStyle"`'s specific missing-default gap is patched. **Verified with a real `cmake && make` + VPK build (`./build.sh`, Debug) — compiles clean, no new warnings.** **Not yet tested on hardware** — the test console (`192.168.3.15`) didn't respond to ping this session.

**2026-07-26/27 — `log_092.txt` (fresh hardware run, first build containing the fix above): the icon column bug is STILL PRESENT, reported directly by the user.** The fix's miss-only condition (`if (!has_option) return 0;`) never logged anything (no diagnostic line existed yet), so there was no way to tell from the log alone whether (a) `hasOption()` actually came back **true** — meaning `"HUDStyle"` really is registered with a genuine value `>=2`, not register garbage from a missing key, which would falsify the "missing default" premise — or (b) the hook simply never fired/matched. `GL_SCISSOR_TEST` is still toggled exactly once at frame 0 in this log (line 35-36), unchanged from `log_091`, confirming the visible symptom itself is unchanged. Plausible alternative explanation for (a), consistent with a pattern already seen elsewhere in this project: the same width `>=960` bucket-selection quirk behind the earlier `g_windowDimensions`/distortion bug (Phase 5) could just as easily make `SavegameManager::_initSettings` register a genuine "tablet" `HUDStyle` default for a 960-wide screen — i.e. the engine may be working exactly as designed for what it thinks is a tablet, not hitting an actual missing-key edge case at all.

**Response (2026-07-26/27):** two changes to `hook_GetSavedOption` in `source/patch.c`: (1) added `l_debug` logging of `savegame_mgr`, `has_option`, and the pre-override raw result for every `"HUDStyle"` read, gated by `DEBUG_SOLOADER` like the rest of this file's diagnostics — the next log will show directly which of the two cases above is real; (2) **the override condition itself was widened from `!has_option` to `result >= 2 || !has_option`** — now forces the fixed phone-style bar (`0`) unconditionally whenever the raw value would have selected the carousel, regardless of whether that value came from a genuinely-registered default or from register garbage. **Verified with a real `cmake && make` + VPK build (`./build.sh`, Debug) — compiles clean.**

**2026-07-27 — `log_093.txt`: theory FALSIFIED. The `"HUDStyle"` hypothesis for this bug is definitively wrong, confirmed with real hardware data, not a guess.** The new `[hook_HUDStyle]` log lines fired dozens of times across the whole session and show, **every single time, with no exception**: `hasOption=1 pre_override_result=0`. `"HUDStyle"` is genuinely registered, its real value is `0`, and the fixed 3-icon phone-style bar branch is what the engine has been selecting all along — the scrollable-carousel-misselection mechanism this whole sub-investigation was built on never happens on this port. The user confirmed the repeating icon column is still present in this same log/session. The `hook_GetSavedOption` hook itself is being **left in place as a harmless no-op safety net** (its override condition never actually triggers, per this log) rather than removed, but **is no longer considered the fix for this bug, or even the correct diagnosis** — see the corrected code comment in `source/patch.c`.

**Corrected understanding of the actual bug, from a fresh look at the real screenshot (`2026-07-25-004631.jpg`):** the repeating element is **not** the numbered combat-action-button icons on the right side of the screen (those are individual, already-identified, and unaffected). It's a **column of stacked rows of 3 identical orange badge/shield icons directly below the character portrait and health/mana bars**, top-left — visually far more consistent with a repeating **buff-icon list** than with the `"HUDStyle"` skill-bar/carousel widget this investigation had been chasing. A fresh static-analysis pass was launched 2026-07-27 specifically re-examining `NativeGetBuffs` (previously given only a cursory, unconfirmed "looks properly bounded" pass) and any array-clearing/reset logic around it, now that the `HUDStyle` lead is closed.

**2026-07-27 — that fresh pass, PLUS a deeper follow-up using a real Flash decompiler (JPEXS FFDec, downloaded for this purpose — Java-based, `ffdec-cli.jar -export script`), closed several more leads and none of them pan out; the icon column's root cause is still not found.**

*Static-analysis pass (re-examining `NativeGetBuffs`, `out_ghidra.c:226280` and neighbors):* confirmed the `BuffInst` deque iteration (`out_ghidra.c:226392-226538`) is genuinely bounded by the real deque — one `push` per real active buff, no over-counting. The array itself is never cleared inside the native function, but that turned out to be this binary's universal convention across the whole `Native*List*` family (`NativeInvGetItemsListForSlot`, `out_ghidra.c:226556+`, does the identical push-only pattern) — clearing a "fresh" array before populating it is the AS caller's job by design, not a native-side bug.

*Decisive new finding, only possible by reading the actual SWF bytecode directly (not the C++ pseudo-C):* extracted and decompiled every AS2 script from the three loaded SWFs (`dqhud.swf`, `dqshared.swf`, `dqmenus.swf`, via `ffdec-cli.jar -export script`) and searched them as plain text. **The literal string `"buff"` (case-insensitive) appears in ZERO of the ~1000+ exported `.as` files across all three SWFs.** As a sanity check, other native HUD bindings that genuinely *are* used (`NativeHUDSpell`, `NativeGetSkillDetails`) show up exactly where expected, confirming the extraction/search methodology itself is sound. **Conclusion: `NativeGetBuffs` is dead code in this build — never called by any ActionScript that ships with the game, regardless of its internal correctness.** This is stronger than the previous "properly bounded, low-confidence" note — it's now closed with certainty.

Also directly read the real `as.hud.Hud` class (`__Packages/as/hud/Hud.as`, exported from `dqhud.swf`) to check the other candidate from the previous static pass, the `DQHUDStatus`/`DQHUDStatus2`/`_LH` family (found as symbol-class names, siblings of `_root`, independent of `HUDStyle`). **Ruled out too:** these are the "item picked up" scrolling-text toast (`itemname_text`, driven by `NativeGetNextStatusMessage`) and the achievement popup (`Achievements_box`, driven by `NativeGetNextAchievementMessage`) — both are single, already-existing, reusable widgets that only toggle `_visible`/play an animation; neither one ever calls `attachMovie`/`duplicateMovieClip`, so even if their native message-queue stubs are buggy (both are near-stub implementations on the native side, confirmed in `out_ghidra.c` — plausible but unconfirmed), the worst they could do is get stuck visible or replay an animation, never stack up N duplicate rows. Searched all `__Packages` classes actually shipped in all three SWFs for any dedicated "icon list"/"badge list" component (the same general-purpose scrollable `list` component the working skill bar uses, `HUDelements.controls.controls.list`, is never separately defined in any exported `__Packages` — it must be an inline clip-attached script, not a reusable class) and found nothing resembling a badge/buff-row repeater.

**Given no AS2 script anywhere in the three loaded SWFs shows any code path that could explain a growing stack of duplicate rows, the next-most-likely mechanism is one of `gameswf`'s own engine-level "create a child clip" primitives being invoked without a matching removal** — `gameswf::sprite_instance::attach_movie` (`attachMovie()`) or `gameswf::sprite_instance::clone_display_object` (`duplicateMovieClip()`), unpaired with `gameswf::sprite_instance::remove_display_object` (`removeMovieClip()`). Rather than guess further from pseudo-C (which would require decoding `gameswf::tu_string`'s exact SSO buffer layout — attempted, but the layout couldn't be confirmed with full confidence from the decompiled constructor alone, and getting it wrong risks a bad hardware-only crash with no way to test it before shipping), three new **count-only** diagnostic hooks were added to `source/patch.c` (2026-07-27): `hook_attach_movie`, `hook_clone_display_object`, `hook_remove_display_object` — all deliberately treat their `tu_string`/`character*` arguments as opaque, never dereferencing them, and instead key a small dictionary purely on the parent `sprite_instance*` (`this`), logging a running `net = creates - removes` count per parent every time one of the three fires (`[clip_diag]` lines). If some parent's `net` count climbs without bound during a session where the icon column is visible on screen, that pinpoints the exact leaking call site by instance pointer and timing, without ever having identified the offending AS script by name. **Verified with a real `cmake && make` + VPK build (`./build.sh`, Debug) — compiles clean.** **Not yet tested on hardware.**

**Bonus data point from the same `log_092.txt`, relevant to the enemy-invisibility investigation (Phase 6 below) even though this log wasn't run for that purpose:** the third render-entry hook added last session, `BaseMeshSceneNode<CSkinnedMeshSceneNode>::render()` (tagged `SkinnedMeshSceneNode` in the log), fired prolifically this run — 19+ distinct instances by frame ~4028 alone, with call counts climbing every frame, vs. the modular hook's single instance/single call. This is a much better match for "the generic path most on-screen characters render through" than either of the first two hooks tested in `log_091`. Not yet cross-referenced against a specific known-invisible enemy instance or against the opacity-field hooks proposed in the "further static-analysis pass" entry above — that correlation is the natural next step once both sets of hooks can be tested together on hardware.

**2026-07-27, following up on the two items above (`log_093.txt` + Phase 6's still-open opacity hooks):** two concrete improvements added to `source/patch.c`, both compiled/verified but not yet hardware-tested:
1. **`MAX_TRACKED_SCENE_NODES` raised 32→128 with an overflow flag** (`render_diag_track`, same class of silent-cap bug already fixed once for the texture tracker in `glutil.c`) — `log_093.txt` showed the `SkinnedMeshSceneNode` hook hit the old 32-instance cap well before the session ended, so the true distinct-instance count was being silently truncated exactly like the earlier texture-tracker bug.
2. **The two opacity-getter hooks proposed in the "further static-analysis pass" entry above are now actually coded**, not just planned: `CharProperties::PROPS_GetOpacity` (`_ZNK14CharProperties16PROPS_GetOpacityEv`) and `Character::GetOpacity` (`_ZNK9Character10GetOpacityEv`), both logging every distinct `this` pointer's opacity value and flagging any exact-zero reading (`opacity_diag_track`/`props_opacity_hook`/`character_opacity_hook`). This directly tests the still-unconfirmed leading hypothesis (a monster's `Opacity_Modifier` property coming out at 0 while the player's doesn't) on the next combat session, and its output can be eyeballed against the `render_diag` `SkinnedMeshSceneNode` instance list from the same log for a rough correlation (neither hook currently shares a common identifier, so exact instance-to-instance matching isn't automatic — timing/count correlation is the best available for now).

Both are `DEBUG_SOLOADER`-gated like the rest of this file's diagnostics, real GL/engine behavior unaffected either way. **Verified with a real `cmake && make` + VPK build (`./build.sh`, Debug) — compiles clean.** **Not yet tested on hardware.**

**2026-07-25: enemy-opacity hypothesis re-investigated twice more, both weakened rather than confirmed — genuinely open, not a dead end.**
- **C++ `CharProperties` opacity-modifier angle: weaker than first thought.** Every `CharProperties` (player and monster alike) goes through one shared `_ResetProperties()` init path pulling defaults from a single static table (`Arrays::CharacterTable::members`) — no "monster init forgot to set opacity" code path exists. The one system that could plausibly zero it post-spawn (`VisualObject::StartFadeIn/Out` and friends) turned out to be empty stub functions in Gameloft's own original binary — dead code on Android too, not something the port broke. Static tracing of the ID-based `PROPS_Set` call sites (as opposed to direct-offset writes) hit a wall — couldn't rule out every write path, but nothing found *confirms* this angle either.
- **Texture-container-format angle: also weakened.** Read the real shipped `GL_Diffuse_L1_iPhone_FS/VS.glsl` shader (extracted straight from `shaders.pak`): it hardcodes vertex alpha to `1.0` and derives final fragment alpha purely from the diffuse texture's own alpha channel (the `AL`/`AT` alpha-map variant's `Object_Alpha` uniform path is dead/commented in the shipped source) — so a monster texture with a broken alpha channel looked like a strong, code-independent explanation. But: these `.tga`-named files are actually a proprietary `BTEXpvr`-tagged PVRTC container, not raw TGA, and a byte-for-byte header comparison between the monster-shared atlas (`atlas_skinned_characters_animdecor_gameobjects_001.tga` — confirmed shared by goblin/troll/root_troll/lizardman-family/werewolf/skydevil/vulture, a real "systemic, not per-monster" candidate) and the player's known-good `atlas_modular_warrior.tga` showed **identical** dimensions, mip count, and pixel-format flag low byte (`0x19`, the field that historically encodes PVRTC alpha-presence) — no header-level smoking gun. A real difference could still exist in the compressed pixel-block data itself, but confirming that needs full PVRTC block decoding, not attempted. Our port's own code has zero involvement either way — `glCompressedTexImage2D` is wired straight to the real driver (`source/dynlib.c`), the closed-source `.so` alone parses this format.
- **New diagnostic added instead of more static reading, per this project's own established methodology ("hit the ceiling on static analysis → test on hardware, don't keep guessing"):** the shared `GL_Diffuse_L1_iPhone` shader template is compiled into different material *permutations* via engine-injected `#define AL`/`AT`/`LM`/`SP`/`FG` combinations (confirmed in the template's own `#ifdef` structure) — if monster materials compile a different permutation than the player's (specifically one enabling the separate `AlphaSampler` alpha-map path), and that second texture unit isn't bound to a valid texture for those draws, GLES2's defined "sampling an unbound unit returns (0,0,0,0)" behavior would zero every affected fragment's alpha exactly as observed. Added three new log-only wrappers (`source/utils/glutil.c`/`.h`, wired in `source/dynlib.c` in place of the raw `glShaderSource`/`glAttachShader`/`glUseProgram` entries, `DEBUG_SOLOADER`-gated internally, real GL calls always run unmodified regardless of build type): `glShaderSource_soloader` tags each shader by scanning its source for an *active* (not `//`-commented) `#define AL`/`AT`; `glAttachShader_soloader` propagates that tag to whichever program the shader gets attached to; `glUseProgram_soloader` logs each newly-used program's tag once (`[gl_shader_variant] program=%u alpha_map(AL/AT)=%d`). Verified with a real `cmake && make` build — clean. **Not yet tested on hardware** — the next combat log's `[gl_shader_variant]` lines, cross-referenced against `gl_diag3`'s periodic `program=%d` sample during monster draws, should finally show whether monsters and the player use different alpha paths.

**2026-07-25: `log_088.txt` (fresh hardware run, `glScissor` fix + shader-variant diagnostic both in place) analyzed — no crash, no aquamarine, stable long session (1441+ sampled frames), combat FPS still 5.6-9.5 (unchanged, expected — still a Debug build).** The `[gl_shader_variant]` diagnostic found exactly **three** programs using the `AL`/`AT` alpha-map path out of ~12 total programs seen (`5880084`, `6300090`, `5670081`) — so this variant is genuinely rare, not the default. One of them (`5670081`) compiled right after `lizardman/animations/big_lizardman_idle_01.bdae` loaded — a real monster — which is suggestive, but shader-compile timing and asset-load timing are two independent subsystems, so this proximity in the log is circumstantial, not proof the lizardman's own mesh draws through that program. **Bigger problem found: `gl_diag3`'s once-per-60-frames single-instant `program=%d` sample landed on `program=210003` (an `alpha_map=0` program, evidently the UI/font one, `tex_binding_2d` sits on a HUD texture at every single sample) across the ENTIRE log, every window, frame 1 through 1441+ — it never once caught an `alpha_map=1` program active, which isn't strong evidence those programs are unused during combat, just evidence that a periodic once-per-60-frame snapshot is the wrong tool to catch something used briefly per frame.

**Fix for the sampling gap: extended the existing per-draw texture tracker (`track_seen_texture()`, `source/utils/glutil.c`) to also record which program was bound at the exact moment each distinct texture was first seen this window**, instead of relying on the separate once-per-window instant sample. `gl_diag_textures`'s output now reports `textureID:pProgramID` pairs (e.g. `4130059:p5670081`) rather than bare texture IDs — this directly answers, per actual texture (including the ~19-27 combat-only IDs already tied to monster draws), which program drew it, closing the gap the periodic sampler couldn't. Verified with a real `cmake && make` build — clean. **Not yet tested on hardware** — this is the most direct diagnostic yet for this investigation: if any of the combat-only texture IDs show up paired with `p5880084`/`p6300090`/`p5670081` (the confirmed alpha-map programs), that's the smoking gun; if they all pair with `alpha_map=0` programs, the alpha-map-shader angle is dead and the remaining open lead is the unconfirmed PVRTC block-level texture-data possibility (`source/utils/glutil.c` change only, `source/dynlib.c` unchanged from the prior entry).

**2026-07-25: `log_089.txt` hit the smoking gun.** Cross-referencing `[gl_diag_textures]`'s new `textureID:pProgramID` pairs during the combat window (frames 781-961) against `log_088`'s confirmed alpha-map program set (`5880084`/`6300090`/`5670081`): five combat-only (enemy-only) texture IDs — `1470021`, `2660038`, `2590037`, `3430049`, `2730039` — are drawn through `p5670081` or `p6300090` (both confirmed `alpha_map=1`) at least part of the time, while the rest pair with plain `alpha_map=0` programs (`p4620066`/`p13650195`/`p10500150`). Notably `1470021` and `2590037` each appear paired with BOTH an alpha-map and a plain program across different sample windows — consistent with the same shared atlas being sampled by different material/submesh combinations of the same monster, some going through the `AL`/`AT` path and some not.

**Next diagnostic added (not yet hardware-tested): does the alpha-map program's second sampler (`AlphaSampler`, conventionally texture unit 1 alongside `DiffuseSampler` on unit 0) actually have a real texture bound?** GLES2 defines sampling an *unbound* texture unit (texture object `0`) as returning `(0,0,0,0)` — if the engine's `AL`/`AT` material path expects something on unit 1 that our port never ends up binding there for these specific draws, every affected fragment's alpha would silently come out as exactly zero, matching the symptom precisely, no GL error anywhere. Extended `track_seen_texture()` (`source/utils/glutil.c`) with a one-shot, read-only probe: the first time a distinct texture is seen paired with an `alpha_map==1` program this window, it additionally reads `GL_TEXTURE_BINDING_2D` on units 0 and 1 (saving/restoring `GL_ACTIVE_TEXTURE` exactly around the two reads, so this changes nothing observable) and logs `[gl_diag_alphamap] texture=%u program=%u unit0_tex=%d unit1_tex=%d`. Verified with a real `cmake && make` build — clean. **Not yet tested on hardware** — if `unit1_tex` comes back `0` specifically for the five enemy texture IDs above during an alpha-map draw, that confirms the exact mechanism and points straight at the fix (binding a dummy fully-opaque 1x1 texture to unit 1 whenever an `alpha_map==1` program is in use and unit 1 is unbound — additive, doesn't touch the closed `.so`, doesn't affect any already-working draw path).

**2026-07-25/26: `log_090.txt` — unbound-alpha-sampler hypothesis WEAKENED, not confirmed.** Every `alpha_map=1` program sample (`5880084`, `6300090`, `5670081`) showed **both** texture units bound to valid, non-zero texture IDs (e.g. `[gl_diag_alphamap] texture=1120016 program=5880084 unit0_tex=1120016 unit1_tex=1190017`) — no `unit1_tex=0` case ever appeared. This argues against "unbound second sampler" as the sole mechanism, though it doesn't fully rule it out for one specific enemy, since no log line ties a program/texture pair to a specific named entity. Also checked and ruled out as a red herring: `depth_test=0` in periodic `gl_diag` snapshots is just sampling-timing (samples land right after the normal per-frame `glDisable(GL_DEPTH_TEST)` that ends the 3D pass), not evidence the 3D pass itself runs with depth test off.

**2026-07-25/26: new investigation angle — which scene-node render() entry point do enemies actually call?** Found via `nm -D`/`c++filt` on the real `.so` that this engine has (at least) three distinct exported skinned-mesh render entry points: `BaseMeshSceneNode<CModularSkinnedMeshSceneNode>::render(void*)` (`_ZN17BaseMeshSceneNodeIN6glitch7collada28CModularSkinnedMeshSceneNodeEE6renderEPv`), `XrayModularSkinnedMeshSceneNode::render(void*)` (`_ZN31XrayModularSkinnedMeshSceneNode6renderEPv`), and `BaseMeshSceneNode<CSkinnedMeshSceneNode>::render(void*)` (`_ZN17BaseMeshSceneNodeIN6glitch7collada21CSkinnedMeshSceneNodeEE6renderEPv`) — all real-symbol, `so_symbol()`-resolvable (not raw Ghidra offsets), so hookable via the existing `hook_addr`/`SO_CONTINUE` mechanism already used for the license-check bypass (`source/patch.c`). Added `DEBUG_SOLOADER`-gated hooks on the first two (`source/patch.c`) that log every distinct scene-node instance + call count seen through each path, forwarding both the implicit `this` and the function's one explicit `void*` render-pass-context argument unchanged through `SO_CONTINUE` (the mangled `renderEPv` signature has that second parameter — an earlier draft of this hook only forwarded `this` and was corrected before testing). **Result, `log_091.txt`:** across a 1180+ frame session with 688 distinct `.bdae` loads spanning 8+ character types (moth, lizardman, troll, faeries, npcs, swampking, root_troll, prince), the modular hook fired for only **2** distinct instances total (one of them exactly **once**, never again) and the Xray hook fired **zero** times. This strongly suggests neither hooked path is the generic "every character" render call despite the note above (line ~456) that both mesh-node types were thought to delegate to an identical shared render — added a **third** hook, on `BaseMeshSceneNode<CSkinnedMeshSceneNode>::render(void*)` (the non-modular variant, the more likely candidate for fixed-mesh monsters), same pattern. Verified with a real `cmake && make` build — clean. **Not yet tested on hardware** — the next log's `render_diag` lines should show whether enemy renders actually go through this third path, and whether a specific known-invisible enemy's instance appears there at all (vs. never rendering through any hooked path, which would point further upstream to spawn/mesh-buffer-population code instead of the render call itself).

**2026-07-26: further static-analysis pass (no hardware available, console unreachable), digging into the opacity write path and the monster spawn path instead of more GL-state diagnostics.** Findings, all from `out_ghidra.c`/raw `.data` inspection, not guesses:
- **Offset `0xb5c` is not a fixed/dedicated field — it's the generic ID-based property system landing on that offset for one specific property.** `Structs::CharacterProperties::m_dataNames` entry **49** is the string `"Opacity_Modifier"`; its `m_dataOffsets[49] = 0xc4`, and `this + 0xa94 (property sheet base) + 0xc4 + 4 == this + 0xb5c` (confirmed by dumping the real `.data` bytes at `0x00999cf0 + 49*4`). `CharProperties::RecalcProperty(int)` (`003dfe60`) computes the final value **additively**: `_GetDefault(49) + base(this+8) + saved(this+0x38c) + gears(this+0x710) + Σ active buffs affecting property 49` (via a `BuffInst` deque walk), gated by a per-property "needs recalc" type-flag from the same data table. **No player-vs-monster asymmetry exists in this code path** — `_ResetProperties`/`RecalcProperty`/`_LoadFromCharTable` are the exact same functions for every character. `_LoadFromCharTable` copies each class's own 900-byte row (`memcpy(this+4, classID*900 + Arrays::CharacterTable::members + 4, 0x380)`) from a table populated at runtime by `Arrays::CharacterTable::read(IStreamBase*)` — i.e. **external data, not baked into the `.so`**. Conclusion: if this is really an opacity bug, the cause is either bad per-class data in the Troll/Bogwomp `CharacterTable` row, or a buff/script zeroing property 49 at runtime for specific instances — not a missing/asymmetric code path, so no further static win is available here.
- **No dedicated `Monster::`/`EnemySpawner`/`CreatureManager` class exists** — spawning is fully data-driven through the same `CharState`-derived state machine used by the player (`CSSpawn`, `CSPreSpawn`, `CSIdle`, `CSMove`, operating on the generic `Character`/`CharStateMachine`). One concrete lead found: **`CSSpawn::OnFocus`** (`003c35ec`) calls `VisualObject::StartFadeIn()` when a per-state-machine flag at offset `0x2d8` is non-zero — exactly the shape of a "materialize on spawn" fade-in. **But `VisualObject::StartFadeIn/StopFadeIn/UpdateFadeIn/StartFadeOut/StopFadeOut/UpdateFadeOut` are all literal empty-stub bodies (`{ return; }`) in the compiled binary** (already noted as dead code elsewhere in this doc — see the `PushMenu`/`videoDone` entry), so as compiled this call chain is a no-op for everyone; low-priority unless a virtual override elsewhere is somehow reaching real fade logic that Ghidra collapsed to the base symbol. `PROPS_AddBuff` has only 2 static call sites in the whole `.so`, meaning any spawn-fade/invisibility buff affecting Opacity_Modifier is most likely applied from **Lua** (confirmed `sfc::script::lua` bindings exist for other `Character` methods), i.e. outside anything Ghidra can show us — data/script-side, same dead-end conclusion as the opacity path above.
- **Verdict: static analysis has hit its ceiling on this bug a second time** (same conclusion the project already reached once before, see the "Static analysis of *what specific state the game is stuck in* has likely hit its ceiling here" note in Phase 5) — the trail terminates at "data/script-driven, not a missing C++ code path." The only way forward is runtime logging, not more pseudo-C reading.
- **Concrete hook list ready for the next hardware session** (all real, `so_symbol()`-resolvable addresses, same `hook_addr`/`SO_CONTINUE` mechanism already used in `source/patch.c`): `CharProperties::PROPS_GetOpacity()` (`003de790`) and `Character::GetOpacity()` (`003a3724`) — log return value + `this` per call during combat to directly compare a specific invisible enemy's opacity against the player's, live; `CharProperties::RecalcProperty(int)` (`003dfe60`) — filter `param_1==49` (`0x31`) and dump the four intermediate sums (default/base/saved/gears) plus buff contributions; `CharProperties::_SetProperty`/`_AddProperty` (`003deca0`/`003df140`) — filter property id 49 to catch a Lua-driven direct write; `CharProperties::PROPS_AddBuff` (`003e232c`) — log the buff name argument for every call, cheapest way to catch a scripted stealth/materialize buff hitting only specific monster instances; `CSSpawn::OnFocus` (`003c35ec`) — log `*(param_3+0x2d8)` and whether `StartFadeIn` is actually reached, to settle whether that path is truly inert for the affected monsters. None of these have been added to `source/patch.c` yet — this is a to-do list for the next session, not code already written.

### Phase 7 — Audio — ✅ done (2026-07-13)
`GLMediaPlayer`/`Musicplayer` classes handle music/SFX — decompile these two Java classes fully
(already extracted to `decompiled/apk_jadx/sources/`) to find the real asset format and id→file
mapping logic. If the game uses OpenSL ES natively, no JNI audio bridge is needed (VitaGL already
provides some audio, but usually OpenAL is preferred for Vita ports). However, the presence of
`GLMediaPlayer` strongly implies audio is played via Java `MediaPlayer`/`SoundPool`. If so, implement
Vita audio bridge methods in `FalsoJNI` based on the exact signatures of the `play`/`pause`/`stop`
methods found in the Java decompilation.

**Outcome:** Confirmed that native code uses JNI calls back to Java `GLMediaPlayer` (like `playSound(sndId, instance, vol)`). The audio filenames are stored in a massive 442-element String array in `Sounddefs.java` mapped by `sndId`. I extracted this array into `source/sounddefs.h` and modified the FalsoJNI bridge in `source/java.c` to use it for logging exact `.ogg` filenames. Full playback integration (via OpenAL/SoLoud) will be added later if needed, but the audio routing logic is fully mapped and understood.

### Phase 8 — Assets — ✅ done (2026-07-13)
Follow `psvita-port-toolkit`'s Phase 5 decision framework (`references/asset_packaging.md`): pick one
strategy (loose files vs. packed `.apk`-like archive) and confirm which path `GLResLoader`/
`nativeSetPaths`-equivalent actually reads from before assuming cocos2d-x's `nativeSetPaths` pattern
applies here.

**Outcome:** Inspected `GLResLoader.java` and found that the game requests files using `getResourceFull`, `getResourceLength`, and `getResourceBytes`. Unlike cocos2d-x which uses C++ file I/O directly, this engine calls back to Java to load resources! FalsoJNI is perfectly positioned to intercept these calls (which it already does in `source/java.c`). The strategy chosen is **Loose files** in `ux0:data/dungeon-hunter-2/assets/`. FalsoJNI's `dh2_resolve_asset_path` translates Java asset requests directly to standard C file I/O `fopen()` on the Vita's memory card, bypassing the need for a complex `.apk` unzipper or asset packager.

### Phase 9 — Packaging & LiveArea — ✅ done (2026-07-13)
Standard rules from `references/livearea_assets.md`/`vpk_packaging.md` — no game-specific changes
expected here. The `CMakeLists.txt` is correctly configured to package `icon0.png`, `pic0.png`, `startup.png`, `bg0.png`, and `template.xml` into the `sce_sys/livearea` directory of the VPK.

### Phase 10 — Hardware bring-up
Same iterative, one-log-one-bug methodology as both Zenonia ports; Vita3K first to rule out logic bugs
cheaply, real hardware to confirm. Re-read `references/hardware_debugging.md` and
`Zenonia2-vita/port_progress.md` before treating any new crash signature as novel — the pthread/mutex,
JNI-method-not-found, and heap-pointer-sign bug classes documented there are engine-agnostic SoLoader
pitfalls, not Gamevil-specific.

### Phase 11 — Performance (opened 2026-07-23, in progress)
Started once Phase 5 confirmed the game genuinely playable: sustained combat runs at **~8-14 FPS**
(occasional spikes to 20-30 FPS in lighter scenes), well short of the 20 FPS minimum / 30 FPS target
goal. `log_078` isolated this from the separate, much more severe (0.1-0.7 FPS) stalls seen during
loading screens, which are already-documented Debug-logging `fflush()` overhead (see Phase 5's "Slow
resource loading" note), not a gameplay performance problem.

**Groundwork already ruled out / already maxed, don't re-check:**
- CPU/bus/GPU clocks (`source/utils/init.c`) already at the real ceiling: `scePowerSetArmClockFrequency(444)`, `Bus(222)`, `Gpu(222)`, `GpuXbar(166)`.
- Compiler flags already `-O3 -ffast-math` (`CMakeLists.txt`).
- No built-in graphics-quality saved option exists to exploit (`GetSavedOption` is used for `"DPad"`/`"HUDStyle"`/volume/orientation — no `"Quality"`/`"LowSpec"`/equivalent anywhere in `out_ghidra.c`).

**Real, confirmed fix (2026-07-24): the extensive GL-state diagnostic instrumentation added while chasing the rendering bug (Phase 5) was real CPU overhead even in Release builds**, since only the `l_info`/`l_debug` log *macros* were compiled out by `DEBUG_SOLOADER` — the `glGet*()` calls and the 16-float NaN-scan on every `glUniformMatrix4fv` upload that fed those macros still ran unconditionally. Now gated behind `#ifdef DEBUG_SOLOADER` for the actual work, not just the log call. Impact not yet isolated on real hardware (needs a controlled Release-build FPS comparison, not done yet — every FPS number gathered so far is from Debug builds).

**Always-on FPS counter added** (`main.c`, uses `l_error` so it's never compiled out by `DEBUG_SOLOADER`): logs `[fps] X.X frames/sec` every ~3 real seconds via `sceRtcGetCurrentTick`. This is the only way any of the FPS numbers in this section were measured — there was no reliable metric before this.

**Two performance-experiment build variants implemented, wired into a new build-flag system (see §4 below), both hardware-tested with problems found and partially fixed:**
- **`DISABLE_VSYNC`** (`--no-vsync-test`): `eglSwapInterval(display, 0)` right after `eglMakeCurrent`. Diagnostic only (causes tearing) — meant to answer "is our FPS artificially vsync-capped, or genuinely below the refresh rate already". **Hardware result: regressed to the aquamarine/black screen** (reported after `log_082`/`log_083`, no log was captured from this specific run yet). Root cause not yet found — plausible candidates (a real conflict vs. speculative) not yet distinguished: (a) a genuine timing-sensitive interaction between disabling vsync and GSInit's frame-counted step machine (the same class of bug as the `videoDone`/language-force "GSInit timing isn't a fixed frame count" lesson elsewhere in this doc), or (b) simply the wrong build/eboot got tested — see the `eboot.bin`-ambiguity bug below, found and fixed the same session. **Needs a fresh log from a confirmed `--no-vsync-test` build before diagnosing further.**
- **`DOWNSAMPLE_RENDER`** (`--downsample-test [DS_NUM] [DS_DEN]`, default 2/3): renders the whole scene (3D + 2D HUD, via redirecting every `glBindFramebuffer(GL_FRAMEBUFFER, 0)` to an offscreen FBO — `glBindFramebuffer_soloader`) into a reduced-resolution color+depth FBO, then upscales it onto the real screen with one `GL_LINEAR`-filtered fullscreen-quad blit (compiled-once tiny GLSL shader) in `gl_swap()` before the real `eglSwapBuffers`. **Hardware result: "didn't even open."** Likely root cause found and fixed (not yet re-tested on hardware): the color attachment texture used `GL_RGB`, a 3-channel format that isn't a universally-guaranteed color-renderable format on embedded GLES2 drivers (RGBA8 is the one format every GLES2 impl must support as an FBO attachment) — switched to `GL_RGBA`. Added explicit logging bracketing FBO creation (`gl_init()`) to localize a crash precisely if it recurs.
- Neither variant has a confirmed-good hardware result yet — **next step is re-testing both with fresh logs**, not further speculative changes.

**2026-07-24: first genuinely clean Release build (no `DEBUG_SOLOADER`) compiled, still not hardware-tested.** Confirmed the confound this section already flagged ("every FPS number gathered so far is from Debug builds") is real and current: `log_084` (`~8.5-9.5 fps` in combat) is a Debug build — `build.sh`'s interactive prompt defaults to `DEBUG_SOLOADER=ON` unless explicitly answered `n`. In that build, `glEnable_soloader`/`glDisable_soloader` (`source/utils/glutil.c`) call `l_debug()` on **every** `GL_DEPTH_TEST` toggle — several times per frame per `log_084` (the engine flips depth test on/off multiple times a frame, once per 2D/3D pass) — and each `l_debug()` call takes a mutex, does two `sceClibVsnprintf`s, a console print, and (per the existing "Slow resource loading" note above) a synchronous `fflush()` to the log file. None of that exists in a Release build (`l_debug`/`l_info`/etc. compile to nothing — see `source/utils/logger.h`). **Built a Release `.vpk`/`eboot.bin` via `./build.sh` (answered `n` at the debug prompt) so the next hardware session can finally get a real, uncontaminated FPS number before chasing `DISABLE_VSYNC`/`DOWNSAMPLE_RENDER` further** — if Release alone already clears the 20 fps target, neither experimental variant may even be needed. **Not yet tested** — the test console (`192.168.3.15`) didn't respond to ping this session.

**Tooling bugs found and fixed while building the above (real, not hypothetical — each one could have caused a wrong build to be tested):**
- `build.sh`'s CMake variable name was wrong (`-DVPK_OUTPUT_NAME=` instead of the real `VITA_VPKNAME` cache variable in `CMakeLists.txt`) — every flagged build silently produced `dungeon_hunter_2.vpk` instead of its own named variant. Fixed, and `VITA_VPKNAME` promoted to a `CACHE STRING` so it's overridable.
- `build.sh`'s "only the no-flag build touches the canonical `eboot.bin`" logic checked an undefined shell variable (`$BUILD_FLAG`, which only exists in the *wrapper* script, not in `build.sh` itself, which uses `$1`) — the condition was always true, so **every** variant build silently overwrote the generic `eboot.bin` that `manage_vita.py`'s "upload eboot only" shortcut uses. This is a real, concrete candidate for testing the wrong variant without realizing it. Fixed to check `$1`; `manage_vita.py`'s `upload_eboot()` also reworked to let you pick among all `eboot_<variant>.bin` files (mirroring the existing multi-VPK picker) instead of assuming one canonical file.

**2026-08-04: `log_098.txt` analyzed — first log in over a week that actually reaches sustained combat (~2008 frames, multiple checkpoints, several character types), with all three families of diagnostic hooks active. Real findings, not more speculation:**

- **FPS: unchanged, no regression, no improvement.** Steady combat readings cluster 7-15 fps (`5.4` to `15.9` across the session), consistent with every prior baseline in this phase. The two severe sub-1-fps stalls in this log (`0.2`/`0.0`/`0.1`/`0.3 fps`) are all during loading/"continue?" screens (`m_loadStep`/`$percentLoaded: 100`/`continue_text._currentframe`), the already-documented `fflush()` loading-screen overhead — not a new gameplay-perf regression. Nothing new to act on here; still waiting on a controlled Release-build combat comparison.
- **Enemy invisibility: the `PROPS_GetOpacity`/`Character::GetOpacity` hooks are CLOSED as a dead end.** Zero `[opacity_diag]` lines fired in the entire ~2008-frame session — not one call to either getter — despite `render_diag` confirming plenty of character rendering happened (`SkinnedMeshSceneNode::render()` hit 76 distinct instances, `BaseMeshSceneNode<CModularSkinnedMeshSceneNode>` hit 1). Re-verified via `nm -D` on the real `.so` that both symbols resolve fine (real `T`/`W` dynsym entries, not a name-mangling mistake) — the hook mechanism itself is proven working in this exact build (`render_diag`/`clip_diag` both fired). Conclusion: these two getters are weakly-linked (`W`) leftover symbols that the compiler fully inlined at every real call site; hooking this address can never observe those reads. **Replaced in `source/patch.c`** with a hook on `CharProperties::RecalcProperty(int)` (`_ZN14CharProperties14RecalcPropertyEi`, a strong `T` symbol, confirmed not inlined) filtered to property `49` ("Opacity_Modifier"), reading the final computed value directly from the property sheet at `this+0xb5c` right after the real function runs. Compiles clean (`./build.sh`, Debug) — **not yet hardware-tested**, needs a fresh combat log to see whether any instance's Opacity_Modifier comes back exactly `0`.
- **Icon column: the gameswf clip-leak hypothesis is now WEAK, not just unconfirmed.** Only 4 total `[clip_diag]` lines fired in the whole session, all clustered at the very end (frames ~1747-1755, right around a `dh2_000_single.checkpoint`/`dh2_000_0_single_level.checkpoint` save, i.e. a level-transition event) — `clone_display_object` on one parent (`0x872145f8`) growing net 1→2→3 with zero removes, plus a single hit on a second parent. **Critically: this means for the first ~1746 frames of the session — the entire main-menu-through-mid-combat window, i.e. exactly when the icon column is already visible per the reference screenshots — not one `attach_movie`/`clone_display_object`/`remove_display_object` call fired.** If the repeating badge-icon column were being created by one of these gameswf primitives, it would have to fire long before frame 1746. This strongly suggests the badge-icon repetition is **not** created via a gameswf display-list leak at all. The 3-call growth near the checkpoint save is more likely an unrelated (and possibly benign — could just be 3 legitimate level-transition UI elements not yet due for removal at session end) event.
  - **New lead, not yet coded:** re-examined `HUDControls::initCachedChars()`/`HUDControls::Draw()` (`out_ghidra.c:196749`/`196205`) — these are native C++ HUD-caching/draw entry points, separate from gameswf entirely. `initCachedChars()` only caches `DebugCachedCharacter` references to named gameswf clips (buttons, joystick, health bars) for fast lookup — it doesn't loop over or draw badge icons itself, so it's not the direct cause, but it confirms this engine's HUD has a native C++ layer sitting alongside the Flash timeline. Given the AS2-level investigation already found zero relevant classes, the next static-analysis step (not yet done) is reading `HUDControls::Draw()`'s full body and whatever "list"/"badge"/"quickslot" native class backs `HUDelements.controls.controls.list` to check for a native loop-bound bug (e.g. iterating a fixed slot count instead of the real active-item count) — a mechanism that wouldn't touch gameswf's attach/clone/remove primitives at all and would explain why this diagnostic came back empty.

**2026-08-04: `log_099.txt` analyzed (same recalc_diag-instrumented build as `log_098.txt`, fresh new-game start through the tutorial level `dh2_000_0_000_041_level.savegame`). One real correction to the enemy-opacity diagnostic, one still-open regression, and one likely-not-a-bug observation:**

- **`[recalc_diag]` fired 52 times this session — ALL 52 came back `raw=0`, no exceptions.** That's the tell that the raw-offset read (`this+0xb5c`) was the wrong diagnostic: if `raw==0` genuinely meant "this character's alpha is 0", every single character including the player would be invisible, which isn't what's reported (only enemies are). Far more likely, `Opacity_Modifier` is an *additive delta* (0 = "no active opacity-changing buff", the normal case for nearly everyone) and the real final alpha `PROPS_GetOpacity()` returns is computed from combining it with something this raw read never captured. **Fixed in `source/patch.c`:** since the hook on `PROPS_GetOpacity`/`Character::GetOpacity` was removed (closed dead-end, see the `log_098.txt` entry above), the address is no longer patched — so `recalc_property_hook` now also calls `PROPS_GetOpacity` **directly, as a plain function pointer**, on the same `this_`, right after `RecalcProperty(this_, 49)` finishes, and logs both the raw slot and the real scaled opacity side by side. This sidesteps the inlining problem entirely (we're not intercepting an engine call, we're making our own call to the untouched original code). Compiles clean (`./build.sh`, Debug). **Not yet hardware-tested** — the next combat log's `[recalc_diag] ... opacity=%f` field, cross-referenced against which enemy is visibly invisible, is the actual test now.
- **New regression, not yet investigated: intro cutscene now audio-only (black screen, sound plays).** `log_099.txt`'s `video:` trace shows the intro played for 34.84s (`video_frames=91`, `audio_frames=391`) before the user skipped it, but the full first-frame GL trace (`draw_video_frame ENTER` → `gl_swap() returned`) only ever logs once by design (`FIRST_DRAW_LOG` gate in `source/video.cpp`), so the log alone can't show whether frames 2-91 actually drew anything or silently no-op'd/failed. Since `source/video.cpp` is brand-new, untested-on-hardware code (see git status — untracked), this needs its own dedicated session with either a non-gated per-N-frames log or a targeted look at whether something downstream (the main engine's own GL calls, or double-buffering) is clobbering the video quad after the first present. Not investigated further this session — flagged, not fixed.
- **"No visible health bar / stats read as infinite / one-hit death" — likely NOT a port bug, needs user confirmation.** This happened during the tutorial level of a brand-new save (`dh2_000_0_000_041_level.savegame`, `text/tutorial.english` loaded just before). The `[ALOG][_DEBUG_OUT] VISIBLE DAMAGE is visible: true/false` / `MIN DAMAGE: 0` lines seen repeatedly during this fight are **Gameloft's own internal `MenuDebugHUD` debug-toggle prints**, present because this binary ships unstripped (see Phase 1) — not something this port introduced. Traced `MenuDebugHUD::LoadFromFile()` (`out_ghidra.c:205330`): it reads 22 bool toggles from `DebugStats.savegame`, but only *if* that file opens successfully; ours doesn't exist (`fopen(...DebugStats.savegame...) -> 0x0`, confirmed harmless — the whole load is skipped, toggles stay at their default/zeroed state, not some dangerous fallback). So this file-miss is a red herring for the stats symptom, not the cause. Given this is literally the game's opening tutorial fight, the missing health bar / lethal one-hit could plausibly be Gameloft's own **scripted tutorial beat** (common in this genre — an intentional "you're overwhelmed at first" story moment) rather than a stat-loading bug. **Needs clarification:** does the health bar/normal stats appear correctly once past this specific tutorial fight, or does the "infinite stats + instant death" persist into normal gameplay afterward? That answer determines whether this is a real bug worth its own investigation or working as designed.

**2026-08-05: `log_100.txt` analyzed (same corrected recalc_diag build, another full combat session, user reports all symptoms unchanged). One investigation now has a definitive, clean answer — a real close, not another "weakened":**

- **Enemy invisibility — the Opacity_Modifier/opacity hypothesis is now CLOSED WITH CERTAINTY, and it is NOT the cause.** All 52 distinct `[recalc_diag]` hits this session read `raw=0 opacity=1.000000` — **every single one**, no exceptions, confirming last session's suspicion: `Opacity_Modifier` really is just an additive delta that's normally `0`, and the real value `PROPS_GetOpacity()` returns (now called directly, bypassing the inlining problem for good) is a clean, fully-opaque `1.0` for every character recalculated this session, non-visible enemies presumably included among the 52. There is no code path left in this investigation where the opacity/alpha system explains a fully-drawn, invisible monster — three separate angles (texture-atlas alpha-channel header bytes, alpha-map shader/sampler binding, and now the `CharProperties` opacity value itself) have each been checked on real hardware and each came back clean. **This entire line of investigation is done; do not re-open it without a genuinely new symptom.** The remaining candidates from before this got sidetracked are the ones flagged as still-open in the Phase 6 hook list: (a) PVRTC texture data corruption at the compressed-block level (header bytes matched, block contents never decoded and compared — the one texture-side angle never actually finished), and (b) something in the mesh/geometry or culling path itself, downstream of `SkinnedMeshSceneNode::render()` (confirmed the right generic entry point, 75 distinct instances again this session) rather than anything in `CharProperties`.
- **FPS: unchanged, no regression.** Combat readings this session again cluster in the same 7-14 fps band (`7.6`, `13.9`, `9.2`, `8.5`, `7.3`, `8.7`, `6.6`, etc.) once loading finishes. This session had noticeably more severe sub-1-fps stalls than usual (`0.1`-`0.9 fps`, several of them), but every one traced back to the same already-documented `m_loadStep`/`$percentLoaded` loading-screen `fflush()` overhead (this session loaded a boss cutscene — `swamp_king`/`m_boss_dark_queen.wav`/multiple `.luac` skill scripts — and the character menu, both loading-heavy), not a new performance regression.
- **Icon column: reconfirms the gameswf-leak theory is dead.** Only 1 `[clip_diag]` hit in the entire session (vs. 4 last time) — the mechanism is not reproducing in any way proportional to how consistently visible the icon column itself is. The `HUDControls::Draw()` native-HUD lead from the previous entry is still the best open thread and remains unread.
- **Video black screen and tutorial stats question: unchanged, still open, not re-investigated this session** (user confirmed "all still the same" without new specifics) — see the `log_099.txt` entries above.

**2026-08-05 follow-up: geometry/transform ruled out too, PVRTC diagnostic added; "no HP/instant death" traced through both candidate systems with no hack/wrong-default found.**

- **Geometry/transform path already clean, no new code needed to confirm it.** `glUniformMatrix4fv_soloader` (`source/utils/glutil.c`) has been flagging all-zero/NaN/Inf uniform matrices on every upload since Phase 5 (only logs when actually degenerate). Checked all three recent combat logs (`log_098`/`099`/`100.txt`) for its `DEGENERATE` tag: **zero hits across all three.** No skinning/model/camera matrix has ever come back collapsed or garbage during any of these sessions — this candidate is closed the same way the opacity one was, by evidence rather than by elimination alone.
- **New diagnostic added for the one remaining untested link in the render chain: real PVRTC format/data at upload time.** The shipped `GL_Diffuse_L1_iPhone` shader (read directly from `shaders.pak` in an earlier session) hardcodes vertex alpha to `1.0` and derives 100% of fragment alpha from the diffuse texture's own decoded alpha channel — for *every* program variant, not just the rare `AL`/`AT` alpha-map ones. That means a texture whose real compressed pixel data decodes to alpha=0 would reproduce this bug exactly, with zero GL error and a fully-issued draw call, regardless of anything in `CharProperties`. Only the PVRTC container's *header* bytes were ever compared between a working and a suspect texture (Phase 6, 2026-07-25) — never the actual compressed block contents. Added `glCompressedTexImage2D_soloader` (`source/utils/glutil.c`/`.h`, wired into `source/dynlib.c` in place of the raw entry point, `DEBUG_SOLOADER`-gated internally, real GL call always unmodified): logs the real `internalformat` enum (RGB-only vs RGBA-capable PVRTC, confirmed at actual upload time rather than inferred from a static file header) plus width/height/imageSize for every distinct texture upload, and flags the cheapest possible corruption signal — a compressed blob where every single byte is identical (cannot encode real image detail in any format). Verified with a real `cmake && make` + VPK build (`./build.sh`, Debug) — compiles clean. **Not yet hardware-tested** — the next combat log's `[gl_diag_pvrtc]` lines are the next real data point for this bug.
- **"No HP bar / infinite stats / one-hit death" — investigated both systems that could plausibly force a wrong default; neither shows a hack or bug on our end.** User asked to make sure the port isn't accidentally running some non-default/"hacked" state. Traced both native systems that read persistent boolean toggles at boot:
  - `DebugSwitches` (`DebugSwitches.savegame`, read+rewritten multiple times every boot in every log so far): confirmed via `out_ghidra.c` (`DebugSwitches::GetSwitch`/`SetSwitch`, `_loadSwitches`/`_saveSwitches`) that this is a generic `std::map<string,bool>` where any switch not found defaults to `false` the moment it's first queried — there is no "true by default" path anywhere in this class. The specific switches actually queried during boot (`IsDeactivatingFlashMenus`/`...Update`/`...Render`, `ConnectToAlphaServer`/`ConnectToBetaServer`) are unrelated to damage/invincibility, and the repeated open/rewrite pattern is explained by `SetSwitch()` calling `save()` (a full reopen+rewrite) every time a switch's value actually flips — genuine original-game behavior, not something `source/reimpl/io.c`'s `fopen_soloader` (a thin passthrough to the real `fopen()`, confirmed by reading it directly) could be corrupting.
  - `MenuDebugHUD` (the class actually responsible for the `VISIBLE DAMAGE`/`MIN DAMAGE: 0` debug prints seen in `log_099.txt`): its 22-bool toggle array only gets populated `if` `DebugStats.savegame` opens (ours doesn't exist, so the load is skipped — already noted). Confirmed via `MenuDebugHUD::GetInstance()` (`out_ghidra.c:205713`) that the object itself is a **function-local static** (classic Meyer's singleton), meaning it lives in `.bss` and is zero-initialized by the ELF loader *before* any constructor runs, on Vita exactly as on Android — so even with the file load skipped, that bool array was already all-`false` beforehand, not garbage/uninitialized heap memory. No mechanism found here either that could leave a toggle stuck "on" by accident.
  - **Conclusion: no forced-hack/non-default state was found in either system that this port's code is responsible for.** The literal `VISIBLE DAMAGE`/`MIN DAMAGE: 0` prints are Gameloft's own QA logging (present because the binary ships unstripped), not evidence of an active cheat. The leading remaining theory is still the one from the `log_099.txt` entry above: this may be an intentional scripted tutorial beat, not a bug. **Still needs the same clarification asked for last time and not yet answered:** does the missing HP bar / one-hit death happen only during that opening tutorial fight, or does it persist into normal levels afterward? Without that, there's no further static-analysis lead to chase here — this is not a "static analysis hit its ceiling," it's "the two candidate mechanisms were checked and both are clean," so a third hypothesis needs a concrete symptom to test rather than another blind guess.

### Video playback (cutscene black-screen regression) — compared line-by-line against `../Prince of Persia /source/video.cpp`, two diagnostics + one defensive fix added (2026-08-05)

`source/video.cpp` was ported from Prince of Persia Classic's Vita port (same SoLoader lineage, its `video.cpp` proven working on real hardware across several rounds of its own hardware debugging — see that project's `Fixes_Log.md` #16-19), adapted only for this project's different graphics stack (real PVR_PSP2 GLES2 driver + a small dedicated GLSL ES program, vs. the source project's vitaGL/GLES1 fixed-function immediate-mode path — see this file's own header comment for the full list of what changed and what didn't). A full line-by-line comparison against the source project's file found the ported logic itself (NV12->RGB565 NEON conversion, SceAvPlayer memory/file/event replacement callbacks, the CDRAM/PHYCONT dedicated-memblock-per-allocation pattern, the dedicated cutscene-audio output thread) to be faithful and structurally sound — no logic error was found by inspection in the vertex math, blend/depth/scissor/cull state handling, texture format (`GL_RGB`/`GL_UNSIGNED_SHORT_5_6_5`, NPOT-safe `GL_CLAMP_TO_EDGE`/no-mipmap params), or shader compile/link (already confirmed succeeding in every log so far, no compile/link error ever printed).

**One real, if latent, bug found and fixed defensively:** `draw_video_frame()` never saved/restored `GL_ARRAY_BUFFER_BINDING` before calling `glVertexAttribPointer()` with client-side `verts`/`uvs` arrays. This is a classic GLES2 footgun -- if any buffer is bound to `GL_ARRAY_BUFFER` at the time (which won't be true for the very first intro video, since nothing has bound one yet at boot, but very likely WILL be true for any cutscene played later in the game, since DH2's own 3D rendering is real VBO-based GLES2, unlike the fixed-function/immediate-mode source project this file was ported from), `glVertexAttribPointer`'s last argument silently stops meaning "pointer to this CPU array" and instead means "byte offset into the bound buffer" -- garbage/degenerate geometry with zero GL error, exactly this bug's shape. Fixed in `source/video.cpp`: `GL_ARRAY_BUFFER_BINDING` is now saved and rebound to `0` before setting up the vertex attribs, and restored afterward, matching the same save/restore discipline already used for blend/depth/scissor/cull/program/texture in this function. This cannot explain the *intro* video's black screen specifically (nothing binds a buffer before it plays), but closes the failure mode for every other cutscene in the game.

**Two read-only diagnostics added, since the intro-video black-screen symptom itself needs a hardware answer, not another guess:**
1. **`[video_diag]` Y-plane scan** (`source/video.cpp`, `video_play()`): on the first decoded video frame only, scans the raw NV12 Y-plane bytes (before any conversion) for min/max/all-identical, and explicitly flags "DECODER NEVER WROTE REAL DATA" if it's a uniform all-zero plane. Tests the theory that the hardware AVC decoder isn't actually writing into the CDRAM/PHYCONT memblock `av_alloc_texture` hands it for this project's specific driver setup (PVR_PSP2, not vitaGL) -- an all-zero Y plane converts to solid black through the exact same math whether or not every GL call downstream succeeds, which would look identical to a GL/upload bug but has a completely different fix.
2. **`[video_diag]` framebuffer readback** (`source/video.cpp`, `draw_video_frame()`): on the first frame only, `glReadPixels` a single center pixel right after `glDrawArrays`, before anything else touches the framebuffer or the swap happens. Real, varying color data here would mean the draw genuinely painted a visible image and the black screen has to come from something *after* this point (the swap/present path); solid black here narrows it to the draw/upload/decode side instead.

Together these two split the whole pipeline (decode -> convert -> upload -> draw -> present) into two independently-checkable halves. Verified with a real `cmake && make` + VPK build (`./build.sh`, Debug) -- compiles clean. **Not yet hardware-tested** -- the next intro-video log's two `[video_diag]` lines should finally show which half of the pipeline is actually at fault.

## 6. Checklist

- [x] APK and all 4 `.so` files decompiled, one folder per artifact, under `decompiled/`.
- [x] `.gitignore` excludes the original package, extracted APK, app-data dump, and all decompiled
      output.
- [x] `psvita-porting` skill installed at `.claude/skills/`.
- [x] `porting_tools/` scripts parameterized for this project (paths, `VITA_IP`, log/data dirs).
- [x] Toolchain confirmed on this machine: VitaSDK, `vitaGL`, `kubridge` stub installed;
      `libshacccg.suprx` copied into the repo; space-in-path workaround identified (rsync-to-`/tmp`,
      not a symlink — see Phase 0). Still outstanding: `kubridge.skprx` and `libshacccg.suprx` actually
      installed on the physical test console (device unreachable as of 2026-07-12).
- [x] Loader skeleton bootstrapped (Phase 2): `main.c`/`dynlib.c` written against the real ABI,
      `lib/falso_jni` vendored, project compiles/links/packages a `.vpk` via a real `cmake && make` run.
      Not yet run on Vita3K or hardware — see Phase 2's "not done yet" note.
- [x] `source/java.c` FalsoJNI method table implemented: all 65 confirmed engine→Java callbacks across
      `GLResLoader`/`GLMediaPlayer`/`Musicplayer`/`DungeonHunter2`, cross-checked against jadx + `strings`
      + `out_ghidra.c` (Phase 3). `GLResLoader`'s 3 asset methods do real file I/O (not stubs); actual
      asset packaging/staging is still Phase 8.
- [x] License/DRM bypass confirmed working before anything else (Phase 4).
- [x] First frame renders (confirmed on real hardware 2026-07-18, `log_057.txt`: `PVR_PSP2 EGL context created`, main loop runs 960+ frames with `GL pipeline clean`). ~~Visual output is still wrong (flat aquamarine clear color, no textured/UI content)~~ — **resolved 2026-07-23, see Phase 5's closing entries.**
- [x] Touch/keys confirmed against real `nativeOnTouch`/`nativeKeyDown` behavior on Vita3K/hardware.
- [x] Audio path understood and implemented.
- [x] Asset strategy chosen and implemented (one strategy only).
- [x] `.vpk` installs cleanly on real hardware (LiveArea valid).
- [x] Playable end-to-end on physical PS Vita (confirmed by the user 2026-07-23: menu, character load, dungeon combat, enemies, effects all render; movement/interaction works via touch). **Not yet fully polished** — see Phase 6 (only 1 of 5 combat action buttons confirmed working, enemies reported invisible in one combat encounter, root cause not found) and Phase 11 (sustained ~8-14 FPS in combat, target 20-30 FPS, two perf experiments built but neither confirmed good on hardware yet).
